
# Next.js
.next/
node_modules/
.env*

# Next.js
.next/
node_modules/
.env*

# Next.js
.next/
node_modules/
.env*

.env*

# ignore
node_modules/
.next/
.turbo/
.env*
.vercel
.env*.local
.DS_Store

# .env.example は公開 template（git 管理対象）。
# 上記 .env* / .env*.local パターンに対する negation rule。
!.env.example

# Python venv
.venv/
**/.venv/

# Python caches
__pycache__/
*.pyc

# vision pipeline io
/tools/vision-pipeline/input/
/tools/vision-pipeline/output/

# python venv
.venv/
**/.venv/

# Build cache
*.tsbuildinfo

# Debug/replay output
scripts/output/

# Fine-tuning exports (training data — do not commit)
exports/

# CoAlter M0-6B internal-pair replay input (PII / consent data — never commit)
internal-pair-*.json
/scripts/coalter/internal-pairs/

# Playwright MCP logs
.playwright-mcp/

# Claude worktrees
.claude/worktrees/

# Local preservation / backups (contain PII — never commit)
backups/

# Claude scheduler lock (runtime artifact)
.claude/scheduled_tasks.lock

# Supabase CLI linked project metadata (local runtime)
supabase/.temp/

# Stage 2.3 variant quality review 一時出力 (CEO 厳守: commit しない、PII 含む可能性)
.tmp/
