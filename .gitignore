
# Next.js
.next/
node_modules/
.env*

# Next.js
.next/
node_modules/
.env*

# Next.js
.next/
node_modules/
.env*

.env*

# ignore
node_modules/
.next/
.turbo/
.env*
.vercel
.env*.local
.DS_Store

# .env.example は公開 template（git 管理対象）。
# 上記 .env* / .env*.local パターンに対する negation rule。
!.env.example

# Python venv
.venv/
**/.venv/

# Python caches
__pycache__/
*.pyc

# vision pipeline io
/tools/vision-pipeline/input/
/tools/vision-pipeline/output/

# python venv
.venv/
**/.venv/

# Build cache
*.tsbuildinfo

# Debug/replay output
scripts/output/

# Fine-tuning exports (training data — do not commit)
exports/

# CoAlter M0-6B internal-pair replay input (PII / consent data — never commit)
internal-pair-*.json
/scripts/coalter/internal-pairs/

# Playwright MCP logs
.playwright-mcp/

# Claude worktrees
.claude/worktrees/

# Local preservation / backups (contain PII — never commit)
backups/

# Claude scheduler lock (runtime artifact)
.claude/scheduled_tasks.lock

# Supabase CLI linked project metadata (local runtime)
# 注意: 過去に supabase/.temp/project-ref が production ref (aljavfujeqcwnqryjmhl) のまま
#       tracked で commit されており、fresh checkout で CLI が production-linked になる事故源だった。
#       2026-06-23 に git rm --cached で tracking 解除。これらは local runtime artifact ゆえ commit 禁止。
supabase/.temp/
supabase/.branches/

# Stage 2.3 variant quality review 一時出力 (CEO 厳守: commit しない、PII 含む可能性)
.tmp/

# P2 Step 2 G3-B judge harness 一時出力 (= local result JSON、 commit しない)
tmp/

# Private eval data (raw シフト画像 / 抽出出力 — CEO 厳守: commit しない、ローカル実験専用)
private-eval/

# Shift review demo の原稿画像（visual smoke 用・commit しない）
/public/shift-demo-*.png
