# Reality Judgment Department Matrix（docs 責務契約・runtime object なし）

- 日付: 2026-06-13 / 作成: 契約管理セッション（CEO 方針 2026-06-13 — 部署を第一級の責務契約に）
- 位置づけ: 「何を作るか」（RC/RJ 工程）と直交する「**誰が何を判断するか**」の責務正本
- 規律（CEO 指示）: **runtime Department object は作らない**。docs 上の RACI 契約として owning/consulted/blocking/outputs/missingInputs/traceRefs を明記し、必要なら JudgmentTrace の diagnostic metadata としてのみ持つ。部署 enum を runtime 正本にしない（語彙乱立・判断二重正本の回避）
- 各 slice 完了報告は本表の該当行（Department Responsibility Matrix）を必ず含める（RC2a-2 以降）

---

## 1. 8 部署の責務（CEO 構想）

| 部署 | 見るもの | 主な出力（既存材料への写像） |
|---|---|---|
| **Plan** | 予定・締切・優先度・空き時間・task/block 関係 | schedulePressure / deadlinePressure / openSlotCandidates / taskPlacementNeed ← DayGraph / TaskRealityNode / dayFeasibility |
| **Mobility** | 移動・出発線・origin・route・天気影響 | movementRequired / originInference / departureLines / routeBurden ← **MovementReality（RC2a-2 実体化）** / LSAT / leaveBy 二段 |
| **Energy** | 睡眠・疲労・集中力・時間帯適性・作業負荷 | energyFit / cognitiveLoadFit / eveningCollapseRisk / taskPlacementFeasibility ← dayState estimates / PrepTime / overrunRisk |
| **Context** | 誰と・何のため・重要度・社会的約束・予定の意味 | commitmentSignal / socialWeight / purposeConfidence / contextMissingInputs ← commitmentSignal（RC2a-3）/ verb / governance |
| **Memory** | 過去の遅刻・準備時間・補正・通知反応・見積もり | correctionProfile / prepAdjustment / routeAdjustment / taskEstimateAdjustment ← CorrectionMemory / PRM / driftSelections（B1 消費） |
| **Risk** | 破綻可能性・他人迷惑・不可逆性・締切超過・衝突 | feasibilityRisk / overrunRisk / socialRisk / irreversibilityRisk / blockingReasons ← Feasibility / collapse factors / safetyGates |
| **Permission** | 自動可否・確認必須・表示のみ・下書き・外部送信禁止 | actionBoundary / permissionRequired / blockedReason ← PermissionLevel 0-5 / Action Boundary 8 段 / INV-5 |
| **Communication** | 通知文・提案文・確認質問・相手連絡下書き | userMessage / confirmationQuestion / proposalCopy / externalDraft（外部送信は Permission gate 必須・自動禁止） ← messageType / Proposal Composer |

## 2. RACI の最小契約（各 slice が埋める表の形）

| 項目 | 内容 |
|---|---|
| owningDepartment | 主担当部署（その slice の出力を所有） |
| consultedDepartments | 判断材料を提供する部署 |
| blockingDepartments | 拒否権を持つ部署（特に Permission） |
| outputs | RJ/Graph に渡す RealityAttribute 出力 |
| missingInputs | 部署として不足している入力 |
| safetyGate | permission / redaction / unknown / external のどの gate を通すか |
| traceRefs | JudgmentTrace に残す根拠 |

## 3. 既弱部署の進化先（CEO/GPT 指摘 — backlog として記録）

- **Energy**: `TaskPlacementFeasibility`（task × candidateWindow の energyFit/cognitiveLoadFit/deadlineFit/splitFit/riskFactors）→ RJ4/Task slice。3 案生成の前提
- **Context**: 「同じ 19:00 渋谷でも飲み/商談/病院で判断が変わる」独立判断が弱い → RJ/Context 拡張。当面は commitmentSignal の社会的重みで近似
- **Communication**: 相手への連絡は **draft_only** まで（自動送信永久禁止・Action 境界の強 gate）

## 4. RJ backlog（owning slice 固定・RC2a-2 のブロッカーではない）

CEO/GPT 裁定どおり、以下は RC2a-2 では実装せず owning slice の完了条件に入れる:

| # | backlog item | owning slice | RC2a-2 blocker? |
|---|---|---|---|
| 1 | RealityJudgmentInput（入力束の統一） | RJ1 開始前 | No |
| 2 | MovementOptionComparison（route/eta/reliability/weatherFit/cost） | RC4 / RJ2（fake route/ETA 禁止） | No |
| 3 | PrepReadinessSignal（manual chip / sensor / unknown seam） | RJ2 / RJ3（UI なし・断定しない） | No |
| 4 | TaskPlacementFeasibility | RJ4 / Task slice | No |
| 5 | Proposal Objective（守る=保護 / 楽=回復 / 攻める=前倒し・破綻ライン超過禁止・制約グラフ上の別ルート） | RJ4 必須 | No |
| 6 | Correction Context Schema（origin/destination/transport/route/weather/eventType/timeBand/socialCommitment） | B1 必須 | No |
| 7 | Intervention Reaction Outcome（shown/opened/ignored/dismissed/acted_after/snoozed/corrected/disabled） | B2/R6 + Ledger（うるさい通知化の防止） | No |
| 8 | CrossDayJudgment（tomorrowGraph × todayCarryOver × fatigue/sleep projection） | RJ1/RJ6/B1（明日の成立を明日の graph だけで出さない） | No |

## 5. 適用記録

- **RC2a-2（MovementReality v0）= Mobility 部署の最初の実体化**（`75826356`/`83cd3764`）。
- **RC2a-3（CommitmentSignal v0）= Context 部署の最初の実体化**（`8ea2cbda`/`44720f1d`）。
- **RC2a-4（decisionDebt components + Moment integration）= Risk/Plan joint の最初の集約**（`624003cc`/`d5393ca7`）。
- **RC2a-5（deriveMomentSnapshot = RC2a-5 input-bundle snapshot）= available RC2a materials の束ね（判断入力・判断結果でない）**（`5996f642`）。RC2a-5A で "完全版"/"全部署" の過大表現を訂正。**RC2a-5B（closeout micro-fix）= unconnectedDepartments runtime field 廃止→pipeline_capability missingInputRefs / momentSnapshotId→momentSnapshotCacheKey（identity 強化・basis に schema/derive/derivation 版）/ missingInputRef 最小 field 固定（dedupeKey/displayPolicy/criticality:unknown）/ 全 node 種別 duplicate guard / 日跨ぎ unsupported trace 保持**（本 commit）。
- **RC2a-6（assembleRealityGraph = RealityGraphSnapshot v0 root assembler）= Plan/Risk joint の Graph root 編成**（`3e9f4bb6`）。compile 済み材料（ern/mv/cs/decisionDebt/momentSnapshot）を 1 つの graph root に束ね、2 層 identity（graphBaseId/snapshotId）・InputRevisionSet・derivationVersionSet・missingInputRefs・safetyFlags を完成。**判断結果（Feasibility/Proposal/Permission action）は出さない**。
  - **発見（前提を疑った結果）→ RC2a-6A で解消**: `dayGraphSnapshotId`（v1 `computeSnapshotId` = date + anchor ID 集合 + day 境界 + gap）は **anchor 内容（時刻/場所/companions/rigidity）を含まなかった**。同一 anchor ID 集合での内容変更が dayGraphRevision を変えず、RC2a 識別子チェーン全体（momentSnapshotCacheKey/graphBaseId/snapshotId）が collide していた。**root cause は DayGraph 層**（pre-existing・RC2a 由来でない）。**RC2a-6A で computeSnapshotId を content-aware 化（v2）して根で解消**（realityCore は無変更で継承）。
- **RC2a-6A（DG0: DayGraph Snapshot Identity Root Fix）= identity root の content-aware 化**（`d580d9d0`）。CEO 裁定で RJ1 前の必須修正。`computeSnapshotId` v1→v2: anchor 内容 revision（`computeAnchorContentRevision` = anchorKind/startTime/endTime/rigidity/locationCategory/locationText(hash)/title(hash)/sensitiveCategory/companions(hash)）を末尾 `:c<hash>` で追加。**snapshotId は永続化されていない**（in-memory cache/comparison key のみ・movementSegmentOverlay checkpoint は string 比較）→ migration 影響 0。fnv1a64Hex/canonicalSerialize を `lib/plan/canonicalHash.ts` に切り出し（dayGraph↔realityCore 循環参照回避・graphIdentity は後方互換 re-export）。privacy: 生 locationText/title/companions は NFC→fnv fingerprint 化し snapshotId に raw text を載せない。RC2a chain（momentSnapshotCacheKey/graphBaseId/snapshotId）全体が root 修正を自動継承。旧 [KNOWN GAP] pin test が CLOSED に flip。
- **DG0-A / RC2a-6B（identity privacy closeout micro-fix）= provenance・privacy 境界を締める**（`5659db50`）。CEO 監査: ①**FNV を privacy-safe と言い切らない**（非暗号 fingerprint・低エントロピー text は hash でも推測余地・pseudonymous/sensitive-derived material 扱い・snapshotId は private-derived cache key で debug/log/shared/per-viewer/external へ無制限露出しない・公開 id 化時は HMAC/salt 再裁定）。②**exclusion field の derive 影響を empirical 検証**: confidence/externalUid/userId/confirmedAt/recurrence は derive 読取 0（grep 確認）→除外正当。**sourceId は derive に効く**（commitmentSignal.ts:106 が `sourcesById.get(sourceId).sourceType==="manual"` で rigidity provenance を決める）→ **content revision に scoped hash で追加**（re-sourcing 捕捉）。③同一 sourceId のまま source RECORD の sourceType 編集は sources-map の変化で本 revision に乗らない（sources は buildDayGraph 入力でない・実質不変）= 別 slice 明記。④projection 最小仕様（canonical/key sort/anchor id sort/companions sort/NFC/no raw text/no volatile timestamp/no array index/hash≠proof）を docs+test に固定。
- **RJ1a（RealityJudgmentInput + Feasibility v0 pure evaluator）= Risk 部署の最初の判断器**（`d5027fc7`）。RealityGraphSnapshot を消費し、target（event / day）の成立性を純粋判定。**判断文・提案・出発線は出さない**。核は **4 バケット厳格分離**（confirmedBlocking[これのみ infeasible] / inferredBlocking[最大 feasible_with_risk] / unresolvedCriticalInputs[→unknown] / riskFactors[severity context]）。FeasibilityStatus = feasible/feasible_with_risk/infeasible/unknown、RiskLevel = low/elevated/high/unknown（factor 集約・**確率/%なし**）。v0 confirmed は hard 同士の時間 overlap のみ（aggressive に infeasible を出さない）。permission = action gate context（feasibility 不可でない）/ commitment = severity context（infeasible でない）/ missingInputs = 判断不能理由（失敗理由でない）。RealityJudgmentTrace（judgmentId[snapshotId+scope+kind+version・raw viewerId 不含]/graphBaseId/snapshotId/inputRevisionSet/usedInputRefs/missingInputRefs carry）。sources-map 未配線 = sourcesRevisionPending/sourceRecordRevisionPending 明示（sourceType は capture-time provenance fact = 実質不変・検証済）。FEASIBILITY_JUDGMENT_VERSION は graph manifest と独立（downstream 版で graph identity を揺らさない）。
- **RJ1a-A（confirmed-blocking closeout micro-fix）= confirmed 衝突の厳格化 + field-level trace**（`525e1dce`）。CEO 監査 5 点: ①confirmed hard 衝突を **explicit duration ∧ confirmed-hard fixedness 限定**に厳格化（assumed_default duration / 弱根拠は「重なって見える」止まり→inferred）。②**hard 判定の正本を cs.rigidity → ern.fixedness に修正**（cs.rigidity は Context の判断値=severity context で confirmed 根拠にしない・fixedness inferred/unknown は confirmed にしない）。③usedInputRefs / reason evidenceRefs を **field-level（node#field）**化（code だけで理由作文できない構造）。④feasible/feasible_with_risk path は **synthetic fixture 限定**（実 compile v0 は大半 unknown・aggressive feasible を出さない）と明記。⑤sourceType immutable caveat を trace に維持（capture-time provenance・re-sourcing は sourceId・同一 sourceId の sourceType mutation 未対応・sources-map revision は将来 InputRevisionSet 配線）。ern.timeWindow に durationSource を露出（explicit/assumed_default を判断器へ）。
- **RJ1b（day-scope aggregation + duplicate/equivalent safeguard + trace refinement）= Risk 判断器の day 集約と二重取り込み安全弁**（`de1b1fef`）。①day-scope は event-level judgments を merged buckets で集約（confirmed 1 つでも→day infeasible / unresolved→day unknown / inferred のみ→feasible_with_risk）。trace に **perEventContributions**（per-event status/risk）を追加し day→event→field を辿れる。②**duplicate/equivalent safeguard**: 同一現実イベントの二重取り込み候補（v0 signal = 同一 timeWindow）を **confirmedBlocking に直行させず unresolvedCriticalInput（duplicate_identity_unresolved）に倒す**（衝突か重複か未確定 → unknown）。LLM で同一判断しない。externalUid/title hash/location hash/source dedup は将来材料（ern 未露出・displayLabel は redaction で不可）。③sourceRecordRevisionPending/sourcesRevisionPending を day trace に維持。no proposal/出発線/intervention/通知/確率。
- **RJ1b-A（duplicate/equivalent semantics closeout micro-fix）= 「duplicate と断定しない」+ pairwise relation trace**（`c43f92da`）。CEO 監査: ①**identical timeWindow を duplicate 証拠として扱わない**。それは `exact_time_collision_ambiguous`（真の衝突か二重取り込みか未確定）であって duplicate ではない。reasonCode/helper/docs から `duplicate_identity_unresolved`/`possible_duplicate` 等の断定表現を除去（`duplicate_identity_unresolved` は future・identity evidence 露出後）。②**pairwise relation trace**（PairwiseTimeRelation: relationId[sorted・dedup]/from/to/relationKind[confirmed_time_conflict/inferred_time_tension/exact_time_collision_ambiguous]/relationBucket/evidenceRefs[両 event の #timeWindow/#fixedness/#durationSource]/missingInputRefs/sourceRefs）を trace.timeRelations に追加 → day→event pair→field。③**day-level precedence**: ambiguity downgrade は同 pair の confirmed 化を止めるだけで**別 pair の confirmedBlocking を消さない**（unrelated confirmed→day infeasible 維持）。④**non-identical confirmed overlap 維持**（10:00-11:00 × 10:30-11:30 explicit+confirmed-hard → confirmed・identical のみ ambiguous）。⑤**"same external" と言い切らない**（externalUid 未露出 → ambiguity relation の missingInputRefs に external_identity_evidence_unexposed を明示）。externalUid/source event id/title hash/location hash は将来 duplicate model 材料。
- **RC2b-1（CollapseRiskProfile v0 = failure mode / risk factor map）= Risk 部署の崩れ方の地図**（`88424a3b`）。FeasibilityJudgment + RealityGraphSnapshot を材料に「どこが崩れそうか」を整理。**Feasibility と別軸**（feasibilityStatus を読まない/コピーしない）。failure mode は feasibility の reasons/relations を再投影（time_conflict_confirmed/time_tension_inferred/exact_time_collision_ambiguous/place_unresolved/movement_unresolved/eta_unresolved/leave_by_unresolved/decision_unresolved/source_revision_pending/boundary_spanning_unsupported/high_commitment_if_disrupted/permission_action_gate）。各 mode に category（collapse_source / unresolved / **severity_modifier**[commitment=痛み・risk source でない] / **action_boundary**[permission=collapse source でない]）+ riskContribution + sourceRefs/evidenceRefs(field-level)/missingInputRefs/relationRefs。riskLevel = contribution の max（**確率/%なし**）: confirmed→high / inferred→elevated / 未解決(missing/ambiguity/decisionDebt)→unknown(high でない) / modifier・action_boundary→寄与なし。**missing/commitment/permission/decisionDebt だけで high にしない**。exact_time_collision_ambiguous=duplicate でない。CollapseRiskTrace（collapseRiskId/feasibilityJudgmentId/usedInputRefs/factorRefs/relationRefs/missingInputRefs carry）。COLLAPSE_RISK_VERSION は graph manifest と独立。
- **RC2b-1A（riskLevel semantics closeout micro-fix）= riskLevel(known severity) と completeness を 2 軸分離**（`e36121c4`）。CEO 監査: ①旧 `riskLevel = max(riskContribution)` は **unknown(completeness) を high/elevated/low(severity) と同軸 max** していた誤りを修正。**riskLevel = known severity（collapse_source 由来のみ）**、known severity 無 + risk-relevant 未解決 → unknown / それも無 → low。②**completeness 軸を新設**: riskCompleteness（complete/partial/unknown）+ hasUnresolvedRiskInputs + unresolvedRiskInputRefs。known high + 未解決 → riskLevel high + completeness partial（known severity を潰さない）。③**source_revision_pending** は riskLevel を上げず（none）completeness/confidence に効く。④**confidence = evidence completeness**（成功確率でない・source pending/未解決で high にしない）。⑤Feasibility 分離維持（feasibilityStatus 非コピー）。violations: completeness 不正 / confidence high なのに非 complete / 未解決あるのに low 断定 を検出。
- **RC2b-2（CollapsePropagationMap v0 = impact surface）= Risk 部署の波及範囲の地図**（`376bee89`）。CollapseRiskProfile + FeasibilityJudgment + RealityGraphSnapshot を材料に「崩れたらどこへ広がり得るか」を整理。**CollapseRisk とも別軸**（候補であって因果確定でない）。**directional edge**（earlier→later・時間前方のみ・backward なし）: time_relation_edge / adjacent_event_order_edge / unresolved_movement_edge（carryover/decision_dependency は v0 未実装）。**edgeId は directional（sorted 不使用）= `pedge:<kind>:<from>-><to>`**（対称 PairwiseTimeRelation の sorted relationId と混同しない）。propagationLevel（none/local/downstream/day_scope/unknown・**確率でない**）= **known surface（confirmed/inferred conflict 由来のみ）**: 後続なし→local / 後続 1→downstream / ≥2→day_scope。movement/ambiguous/decision は `unresolvedPropagationInputs`（別軸・known surface にしない）。**exact_time_collision_ambiguous は causality にしない（edge なし）/ movement unresolved は delay 確定にしない（resolved:false candidate）/ commitment・permission・missing だけで propagation 作らない**。CollapsePropagationTrace（collapsePropagationId/feasibilityJudgmentId/collapseRiskProfileId/failureModeRefs/relationRefs/affectedNodeRefs/missingInputRefs carry）。
- **RC2c-1（InterventionEligibility / ActionBoundary v0）= Permission 部署の最初の実体化（安全の背骨）**（`4a550f03`）。RealityGraphSnapshot + FeasibilityJudgment + CollapseRiskProfile + CollapsePropagationMap を材料に「どこまで介入してよいか」を整理。**提案生成ではなく** eligibility / action boundary 層。**現実を読んでも行動の許可ではない**（high risk/infeasible は実行許可でない）。eligibilityLevel（allowed/requires_confirmation/blocked/unknown・**default-deny**: unknown→allowed にしない）。actionBoundary（display_only/draft_only/ask_confirmation/write_anchor/send_message/book_pay/external_communication/blocked・**v0 天井=ask_confirmation**・write_anchor 以上は天井にしない）。**強 gate**: otherPeople/reservation/payment/work/sensitive → requires_confirmation 以上。**exact_time_collision_ambiguous → 自動 move/skip しない**（canSuggestMove/Skip false・ask_clarification/observe）。canSuggest{Move/Shorten/Skip/Delegate/AskClarification/Prepare/Observe}（change 系は blocked/unknown/ambiguity で停止・delegate は v0 false）。sourceRevisionPending → confidence(=evidence completeness) を下げるが permission を緩めない。display_only も redaction gate（displayRedactionRequired・evaluator は redact 済み label/id のみ参照）。ern に sensitive:boolean 露出（強 gate 用・boolean のみ）。EligibilityTrace（eligibilityId/feasibilityJudgmentId/collapseRiskProfileId/collapsePropagationId/usedInputRefs/missingInputRefs carry）。INTERVENTION_ELIGIBILITY_VERSION は graph manifest と独立。
- **RC2c-1A（sensitive / allowed-boundary closeout micro-fix）= 意味論の固定 + unknown-gate default-deny**（`1f1607ee`）。CEO 監査: ①**ern.sensitive → ern.sensitiveFlagged に改名**（true=flagged 強 gate / **false=未検出≠confirmed safe**・false を allowed の根拠にしない・flag 自体も sensitive-derived material）。reason `sensitive_content`→`sensitive_flagged`（field-level `#sensitiveFlagged` trace）。②**unknown 系 gate を false 扱いしない**（最重要）: gate は confirmed-absent（status≠unknown ∧ value===false）の時のみ clear、unknown/missing は engaged（`*_unverified` reason → requires_confirmation）。**allowed は全 gate confirmed-absent の時のみ**（v0 は gate がほぼ unknown のため allowed はほぼ到達しない=安全動作）。③**allowed/draft_only の意味論固定**: allowed は action permission でない / draft_only は将来の最大境界で **RC2c-1 は draft も提案も生成しない** / write_anchor 以上は常に不可 / draft_only を UI・実行層へ直結しない。④sourceRevisionPending は confidence を下げるが permission を緩めない（継続）。⑤**display permission ≠ action permission**: display_only も per-viewer redaction / evidence visibility gate（evidenceRefs は field 識別子のみ・raw content 不含）。violations: gate(present/unverified) があるのに allowed を検出。
- **RC2c-2（InterventionDecision / ContactPolicy v0）= Permission 部署の介入判断内部状態（抑制の層）**（`759cd614`）。Eligibility を受けて今この瞬間に **silent / observe / ask_clarification / internal_prepare / blocked** を決める内部状態。**文面/通知/提案を生成しない**。decisionKind（exposure 順 silent<observe<internal_prepare[無接触]<ask_clarification[接触]）は **eligibility.actionBoundary を超えない**（cap: display_only→observe / draft_only→internal_prepare / ask_confirmation→ask_clarification / blocked→blocked）。**silent≠observe**（observe は nextEvaluationAt/reevaluationTrigger/stopCondition を持つ・silent は持たない）。**抑制**: eligibility requires_confirmation でも実質理由（confirmed gate/ambiguity/infeasible）が無ければ ask せず observe（unknown だから勝手に聞かない）。**gate 下では internal_prepare-to-action しない**（gate あれば ask or observe・prepare は gate 無し時のみ）。exact_time_collision_ambiguous→ask_clarification or observe（move/skip にしない）/ sourceRevisionPending only→observe / high collapse・infeasible は ask_clarification 候補だが通知/提案/write_anchor にしない。contactPolicy（none/internal_only/ask_permission_required/blocked）+ deliveryModeCeiling（v0 は active_prompt/push/external なし）は**将来の接触上限**で配信命令でない。whyNowFactors/whyNotFactors。InterventionDecisionTrace（decisionId/eligibilityId/eligibilityRefs/全 chain id/missingInputRefs carry）。INTERVENTION_DECISION_VERSION は graph manifest と独立。
- **RJ2-0（Judgment / Proposal Surface Boundary Design・docs-only）= 内部判断 → user-facing surface の境界正本**（`92bce673`）。設計書 `docs/reality-judgment-surface-boundary-rj2-0.md`。**実装なし**（型 skeleton と境界宣言のみ・コード/copy/配信は全て HOLD）。多視点設計 6 断片 + 敵対 red-team 6 件（穴 blocker 2/major 4 発見）+ 統合で生成。確定 gate pipeline: **G0 KILL → G1 DECISION → G2 PERMISSION → G2.5 MOVEMENT-INPUT(新規) → G3 EVIDENCE/CLAIM → G4 REDACTION(sensitive を自身で generic 化) → G5 DELIVERY**。出力層 L0-L9 分離(v0 は L1/L2/L3 のみ・L4-L9 HOLD)。INV-0(core 直結禁止・surface plan 経由)/INV-4(exposure≤decisionKind≤actionBoundary)/INV-5(L0↔L1 不可逆境界)。red-team 封鎖: leaveBy=null departure 構造遮断(G2.5)・ambiguity prepare strip・proposal 床=ask_clarification・G4 sensitive genericize・clarificationOnly を decisionKind hard-gate・feasibility_state assert 禁止。型案 7 種(JudgmentSurfacePlanV0/ClarificationQuestionCandidateV0/SurfaceClaimV0/ProposalCandidateBoundaryV0/DepartureLineBoundaryV0/SurfaceRedactionPolicyV0/SurfacePermissionGateV0)。次 slice 案 RJ2a-f(RJ2e copy/RJ2f notification は HOLD+CEO 承認)。owning=Communication・blocking=Permission+CEO。**RJ2a GO はまだ無い**(CEO 確認待ち)。
- **RJ2-0A（surface gate contradiction closeout・docs-only）= RJ2-0 設計内 4 矛盾の補正**（`4e55ccb0`）。設計書 §11 + INV-9/10/11。①**notActionable 再裁定（採用 A: 表示可・操作不可）**: G0 KILL から notActionable 除去（kill は blocked のみ）・L1 show_redacted + actionAffordance none・clarificationOnly false/proposableShapes []/departureLineRefs []/notification 不可/redaction 必須。②**ambiguity strip と clarification hard-gate の矛盾解消**: ambiguity は clarification 材料だが clarificationOnly=true を強制しない（clarificationOnly は decisionKind===ask_clarification のみ・observe+ambiguity は clarification surface 出さない）。③**L1/L2「出してよい」= object 構築可であって user 表示でない**（RJ2a/b/c は plan/claim/question candidate object・claimTextDraft null・文面なし・user-facing emission は RJ2e+ CEO 承認まで HOLD）。④**active_prompt/deliveryModeCeiling は dispatch でない**（G5 で v0 落とす・notification/contact は RJ2f まで型すら実行しない）。自己判定: RJ2a は設計 ready だが GO は CEO 専管。実装/build なし。
- **RJ2a-0（JudgmentSurfacePlan Implementation Design・docs-only）= RJ2a 実装境界の確定**（`e0376aa1`）。設計書 `docs/reality-surface-plan-impl-design-rj2a-0.md`。**コードなし**（設計提出のみ・RJ2a 実装 GO は CEO 専管）。①対象ファイル: 追加 `judgmentSurfacePlan.ts` + test・**既存 6 判断器ファイル不接触**。②実装型 5 種（JudgmentSurfacePlanV0/SurfaceExposureLevel/SuppressedSurfaceRef/SurfacePlanTrace/string violations）・ClarificationQuestionCandidateV0 は ref が string id ゆえ RJ2a 不要→RJ2c defer。③deriveSurfacePlan 入力 6 + integrity guard 8 段・出力は exposure 包絡（exposureLevel=min(decisionKind,actionBoundary 由来)・clarificationOnly=decisionKind===ask_clarification のみ・proposalCandidateRefs/departureLineRefs/allowedClaimRefs/clarificationCandidateRefs v0 []・copy/notification/action field なし）。④gate: G0-G2.5 実装（notActionable で kill しない・departureLineRefs 構造遮断）/ G3-G5 carry-placeholder。⑤walker 13 + carry（real/RJ2b-defer 区別）。⑥fixtures 14 件（blocked/silent/observe/internal_prepare/ask_clarification/ambiguity→clarificationOnly false/leaveBy null→departure []/notActionable 非 kill/active_prompt 非 dispatch/graphViewerKey 非 authority/claimTextDraft 不在/guard/IO scan）。⑦HOLD: claim/question/proposal/departure/copy/notification 生成。owning=Communication・blocking=Permission+CEO。自己判定: RJ2a 実装設計 ready だが GO は CEO 待ち。
- **RJ2a-0A（surface plan exposure closeout・docs-only）= internal_prepare exposure 矛盾の補正**（`dee473ac`）。設計書 §11 + RJ2-0 §7-A 型更新。CEO 監査: `internal_prepare → passive_only` は危険（passive_only は L1 user-facing と読め誤表示リスク）。①**SurfaceExposureLevel を 4 値に**: none/**internal_only**/passive_only/ask_eligible（非線形・internal_only は user-facing 0）。internal_prepare → internal_only。②導出を **min-rank → decisionKind 直接写像**に変更（internal_only と passive_only は別枝で min が化ける）。displayPolicy: internal_only も notActionable。③**suppressedSurfaces 4 状態分離**（silent/blocked/internal_prepare[internal-only boundary・準備は allowed]/observe）— internal 準備可能を silent/blocked と同理由で潰さない。④walker +5（internal_prepare で passive_only/user-facing refs/clarificationOnly=true/写像不一致/passive 扱い→FAIL）。⑤fixtures: #4 を internal_only・no user-facing refs に修正 + #15/#16 追加。自己判定: RJ2a 実装設計 ready（補正後・内部無矛盾）だが GO は CEO 待ち。code なし。
- **RJ2a（JudgmentSurfacePlan 実装）= 判断 → surface 露出境界の包絡**（`1988a877`）。新規 `lib/plan/realityCore/judgmentSurfacePlan.ts` + `tests/unit/judgmentSurfacePlan.test.ts`（既存 6 判断器不接触・consume only・一方向）。型: `JudgmentSurfacePlanV0` / `SurfaceExposureLevel`(4 値) / `SuppressedSurfaceRef` / `SurfacePlanTrace` / `DeriveSurfacePlanInput`。関数: `deriveSurfacePlan`(6 入力 + 9 段 integrity guard + scope 一致)・`surfacePlanViolations`・`exposureForDecisionKind`。exposure 写像: blocked/silent→none・observe→passive_only・internal_prepare→**internal_only**(user-facing でない)・ask_clarification→ask_eligible(直接写像・min-rank ではない)。**CEO 防御ガード実装**: walker で exposureLevel が actionBoundary ceiling 超え→FAIL / ask_eligible は ask_clarification かつ boundary 許可時のみ / display_only・blocked で ask_eligible 不可 / draft_only を user-facing ask に進ませない。v0 固定: allowedClaimRefs/clarificationCandidateRefs/proposalCandidateRefs/departureLineRefs=[]・redactionPolicyRef/permissionGateRef=null・copy/notification/contact/dispatch/action field 型に無し。suppressedSurfaces 4 状態分離。walker 19 + 構造 assert(FORBIDDEN_FIELDS)。**17 tests PASS**・tsc baseline 55 維持(additive・新規ファイル 0 error)・full suite FAIL 2 のみ(realitySeedSource/realityDurationEvidenceSource・A1-5-x static-safety・realityCore 外)・next build PASS。既存 6 判断器/ern/cs/mv/snapshot/identity 不接触・UI/storage/API/DB/location/notification/external read 不接触・tree clean・production gate 未通過。RJ2e(copy)/RJ2f(notification) HOLD 維持。次は **RJ2b 実装に進まず停止** → RJ2b-0 Claim Evidence/Redaction 実装設計を docs-only 提出。
- **RJ2b-0A（claim binding / walker contract closeout・docs-only）= claim binding と walker 責務の曖昧補正**（`631e662d`）。設計書 `…rj2b-0.md` §11 + §1/§1.1/§2.1/§3.4/§5/§6/§8/§10 改訂。CEO 監査: `surfacePlanViolations(plan)` 単体では claimSet の中身（claimId 実在/withheld/exposureBinding/verdict 除外/claimTextDraft）を検証できず walker が形だけになり claimSet が plan を迂回する穴になる。①`surfacePlanViolations(plan)` を **plan 単体検証に留める**（RJ2a 現行を不接触で流用）。②**3-walker に責務分離**: `surfaceClaimSetViolations(claimSet)`（claim 単体）+ `surfaceClaimBindingViolations(plan, claimSet)`（整合）追加。③walker #6' 緩和を**撤回** → `allowedClaimRefs` v0 [] 据え置き・**RJ2b は judgmentSurfacePlan.ts 完全不接触**（純 additive・新規 1+test 1 のみ）。④`bindClaimsToPlan` は plan を mutate せず検証済 `BoundSurfaceV0` を返す。⑤`SurfaceClaimSetV0` 単体は consumer payload でない・direct read 禁止・validated binding 必須（projection は RJ2d+）。⑥`exposureBinding ≤ plan.exposureLevel` は `userFacingExposureRank`（none=internal_only=0/passive=1/ask=2）比較・internal_only と passive_only を混ぜない（internal_only は claim 経路に乗せず claims [] で早期遮断）。⑦`confirmation_needed` claim ≠ question candidate（文面なし・ClarificationQuestionCandidate でない・ask_eligible 以外で生成しない・ambiguity 単独で claim 化しない）。自己判定: RJ2b 実装設計 ready（補正後・claimSet 迂回穴を binding walker で封鎖）だが GO は CEO 待ち。code なし。
- **RJ2b（SurfaceClaim 実装）= 判断 → 主張してよいことの構造化 envelope**（`e9e482b0`）。新規 `lib/plan/realityCore/surfaceClaim.ts` + `tests/unit/surfaceClaim.test.ts`（**judgmentSurfacePlan.ts + 既存 6 判断器不接触**・consume only・一方向・純 additive）。型: `SurfaceClaimV0` / `SurfaceClaimSetV0` / `SurfaceClaimType`(verdict 除外 5 種) / `ClaimAssertability`(4) / `ClaimEvidenceContract` / `ClaimRedactionPolicy` / `BoundSurfaceV0` + trace。関数: `deriveSurfaceClaims`(5 入力 + integrity guard) / `surfaceClaimSetViolations`(claim 単体) / `surfaceClaimBindingViolations`(整合) / `bindClaimsToPlan`(failure-loud)。**claim = envelope であって文面でない**。最重要安全則を構造担保: ①feasibility verdict を claimType に含めず assert 経路を構造排除(FORBIDDEN_VERDICT_TOKENS 二重検証)・②claimTextDraft=null(copy RJ2e HOLD)・③evidenceVisibility=internal_trace_only(leak 防止)・④sensitive genericize(subject id のみ・category 非露出)。**CEO 3 追加ガード実装**: claimId uniqueness(set/bind 両方で重複 FAIL)・BoundSurfaceV0 も consumer payload でない(direct read 禁止)・bindClaimsToPlan は違反時 throw(静かに捨てない)。assertability cap: assertable は confirmed bucket のみ/inferred→hedged/unresolved→observation_only/confirmation_needed→hedged 上限・exposure cap(passive_only→hedged)。exposureBinding ≤ plan.exposureLevel は userFacingExposureRank 比較(judgmentSurfacePlan の private をローカル再定義・不接触)・internal_only は claim 経路に乗せず claims [] 早期遮断。confirmation_needed は ask_eligible のみ・claim であって question でない(RJ2c 分離)。exposure none/internal_only → claims []。**21 tests PASS**・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・next build PASS。judgmentSurfacePlan.ts + 既存 6 判断器/ern/cs/mv/snapshot/identity 不接触・UI/storage/API/DB/location/notification/external read 不接触・tree clean・production gate 未通過。RJ2e(copy)/RJ2c(question 文面)/RJ2d(proposal/projection)/RJ2f(notification) HOLD 維持。次は **RJ2c 実装に進まず停止** → RJ2c-0 ClarificationQuestionCandidate 実装設計を docs-only 提出。
- **RJ2c-0A（question identity / dedupe / unresolved scope closeout・docs-only）= 危険な dedupe/scope の補正**（`cfe2d796`）。設計書 `…rj2c-0.md` §11 + §1/§2.1/§3.3/§5/§9/§10 改訂。CEO 監査: ①`dedupe by questionKind`（同 kind 1 件）は予定 A/B の otherPeople を 1 件に潰し source trace/evidence/targetScope を失う → 危険。②`resolve_unresolved_input` が広すぎ leaveBy/eta/route を question 化すると departure/ETA 入口。補正: ①**dedupe by questionKind を撤回** → questionId/dedupe を (surfacePlanId+questionKind+subjectScope+subjectNodeId+relationRef+gateReasonCode+evidence basis+version) に再定義（同 kind でも subject/relation/evidence 違えば別 question・dedupe で evidenceRefs/sourceRefs/missingInputRefs を union 保持）。②subjectNodeId は **gate reason の targetNodeId**（per-event）・time collision は **relationRef 単位**（fj.timeRelations の relationId）。③`resolve_unresolved_input` を **allowlist 化**（`ALLOWED_UNRESOLVED_FOR_QUESTION`・v0 は movement/eta/leaveBy/route/place/source-pending/duplicate を全除外 → suppressedQuestionRefs/missingInputRefs）。④question に `relatedClaimRefs`+`gateReasonCode` 追加（confirmation_needed claim/gate へ trace・claim を入力に取らず mutate しない）。⑤`answerShape` は internal structure only（label/choices/yes-no なし・disambiguate_two_way は relationRef 付きで両義を開く）。⑥questionSet direct read 禁止（consumer payload でない・projection は RJ2d+）。walker +3（relationRef null/gateReasonCode null/allowlist 外）。自己判定: RJ2c 実装設計 ready（補正後・per-event/per-relation で trace 保持・unresolved allowlist で departure 入口遮断）だが GO は CEO 待ち。code なし。
- **RJ2c（ClarificationQuestion 実装）= 判断 → 何について確認するかの構造化 slot**（`d100a76e`）。新規 `lib/plan/realityCore/clarificationQuestion.ts` + `tests/unit/clarificationQuestion.test.ts`（**judgmentSurfacePlan.ts 不接触・surfaceClaim.ts は consume のみ[SURFACE_CLAIM_VERSION 参照・不変]・既存 6 判断器不接触**・純 additive）。型: `ClarificationQuestionCandidateV0`(+relationRef/gateReasonCode/relatedClaimRefs) / `ClarificationQuestionSetV0` / `ClarificationQuestionKind`(6) / `QuestionEvidenceContract` / `QuestionRedactionPolicy` + trace。関数: `deriveClarificationQuestions`(5 入力 + integrity guard) / `clarificationQuestionSetViolations`(15 項) / `clarificationQuestionBindingViolations`(10 項)。**question = 確認 slot であって文面でない**。最重要安全則を構造担保: ①**hard gate**(exposureLevel ask_eligible ∧ clarificationOnly ∧ carriedDecisionKind ask_clarification の 3 揃い・CEO 追加ガード)・②questionTextDraft=null + answerShape internal structure only(label/choices/yes-no なし・FORBIDDEN_FIELDS)・③**leaveBy/departure 逆算 question を作らない**(questionKind に無い・常に suppress)・④**exact_time_collision_ambiguous を duplicate 断定しない**(assertsDuplicate=false・**relationRef 必須**・null→FAIL・CEO 追加ガード)・⑤**unresolved allowlist**(`ALLOWED_UNRESOLVED_FOR_QUESTION`・v0 空=movement/eta/leaveBy/route/place/source-pending/duplicate を全除外→suppressedQuestionRefs/missingInputRefs・CEO 追加ガード)・⑥evidence internal_trace_only・sensitive genericize(category 非露出)。identity=full tuple(surfacePlanId+kind+scope+subjectNodeId+relationRef+gateReasonCode+evidence basis+version)・**同 kind でも subject/relation/evidence 違えば別 question**・dedupe で evidenceRefs union。relatedClaimRefs=**trace hint**(RJ2b confirmation_needed claimId を決定的算出・存在証明でない・claim 不 mutate・実在照合は RJ2d・CEO 追加ガード)。**20 tests PASS**・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・next build PASS。judgmentSurfacePlan.ts/既存 6 判断器/ern/cs/mv/snapshot/identity 不接触・UI/storage/API/DB/location/notification/external read 不接触・tree clean・production gate 未通過。RJ2e(文面)/RJ2d(proposal/projection)/RJ2f(notification) HOLD 維持。次は **RJ2d 実装に進まず停止** → RJ2d-0 SurfaceProjection / Boundary Integration 実装設計を docs-only 提出。
- **RJ2d-0A（SurfaceProjection ConsumerView/InternalTrace/SafeKind closeout・docs-only）= consumer-facing 境界の漏洩補正**（`4cb325a3`）。設計書 `…rj2d-0.md` を RJ2d-0A revised design に全面改訂（§0/§2/§3/§5/§6/§9/§10/§11）。CEO 監査 8 点 + **内部 red-team（4 レンズ・29 leak vector・26 が CEO 8 点外・15 high・ultracode workflow `wf_0802478c`）**を反映。中核裁定 = **「strip 方式」→「allowlist 構築方式」転換**（consumer view は安全 field のみ 1 つずつ明示構築・内部 object を spread/copy しない → field 追加 leak-by-omission を構造的に不可能化）。①`SurfaceProjectionConsumerViewV0`(consumer 唯一の出口・trace/id なし) と `SurfaceProjectionInternalBundleV0`(internal) に**型分離**。②**consumer-safe kind 完全変換**(internal 11 kind→consumer 7・4 gate question を `needs_verification` 1 値に潰し sensitive/work/reservation/otherPeople 区別不能化)。③consumer view から decision metadata 全除去(assertability/derivedFromBucket/derivedFromGate/gateReasonCode/why*/relatedClaimRefs/evidence/sourceRefs/redactionReason/missingInputRefs/answerShape/displayPolicy/per-item genericized/redactionApplied)。④genericized 時は条件付き collapse でなく **sensitivity を観測不能化**(dual-channel leak 排除)。⑤opaque ref は projection-local・**配列出現順採番**(sort 採番は時刻順序漏洩ゆえ回避)・非相関。⑥projectionId は InternalBundle のみ・consumer view は **id なし**。⑦入力に **BoundSurfaceV0 採用**(RJ2b validated bundle 再利用・案 A)。⑧boundary は **boolean のみ**(reason/slice 名/version を consumer に出さない)。⑨**serialization backstop test**(JSON.stringify に禁止トークン非出現)。walker=許可 key 完全一致 + 禁止トークン scan + binding。自己判定: RJ2d 実装設計 ready(allowlist+型分離+safe kind+metadata 全除去+backstop で 8 点+26 追加 vector 構造封鎖・残留は件数/表示可否のみ §3.4 明示)だが GO は CEO 待ち。code なし。
- **RJ2d（SurfaceProjection 実装）= 内部 object → consumer-facing payload の唯一の境界**（`8f2c9130`）。新規 `lib/plan/realityCore/surfaceProjection.ts` + `tests/unit/surfaceProjection.test.ts`（**RJ2a/b/c 3 ファイル不接触・既存 6 判断器不接触**・consume only・純 additive）。型: `SurfaceProjectionConsumerViewV0`(consumer 唯一の出口) / `SurfaceProjectionInternalBundleV0`(internal) / `ProjectedClaimView` / `ProjectedQuestionView` / `ProjectedClaimKind`(4) / `ProjectedQuestionKind`(3) / trace。関数: `deriveSurfaceProjection`(BoundSurface+questionSet→InternalBundle・**failure-loud**) / `surfaceProjectionConsumerViewViolations` / `surfaceProjectionBindingViolations`。**consumer 境界だが文面ではない**(copy RJ2e HOLD)。最重要安全則を構造担保: ①**allowlist 構築**(内部 object を spread/copy せず安全 field のみ明示構築・leak-by-omission 不可)・②**ConsumerView/InternalBundle 型分離**(trace/id/metadata は bundle のみ)・③**consumer-safe kind 完全変換**(internal 11→consumer 7・4 gate question を `needs_verification` 1 値に潰し sensitive/work/reservation/otherPeople 区別不能化)・④id/trace/decision metadata 全除去(assertability/derivedFrom/gateReasonCode/why*/evidence/sourceRefs/redactionReason/genericized/displayPolicy/exposureLevel(enum) なし)・⑤opaque projection-local ref(配列出現順採番・raw へ戻せない)・⑥boundary boolean only(reason/slice 名/version を consumer に出さない)・⑦**serialization backstop**(JSON.stringify に禁止トークン 22 種非出現を walker で検証)。**CEO 追加ガード = 5 walker validated binding 全実行**(plan 単体/claimSet 単体/claim binding/questionSet 単体/question binding・BoundSurfaceV0 は TS 上偽造可能ゆえ上流を信頼しすぎず全層再検査・どれか violation で throw)。BoundSurfaceV0 採用(RJ2b validated bundle 再利用)。**17 tests PASS**・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・next build PASS。RJ2a/b/c/既存 6 判断器/ern/cs/mv/snapshot/identity 不接触・UI/storage/API/DB/location/notification/external read 不接触・tree clean・production gate 未通過。RJ2e(copy)/RJ2f(notification) HOLD 維持。次は **RJ2e 実装に進まず停止** → RJ2e-0 User-Facing Copy / Language Surface Design を docs-only 提出。
- **RJ2e-0A（Copy Template Catalog / Lexical Safety closeout・docs-only）= exact 文面正本化 + 論理修正**（`6252400f`）。設計書 `…rj2e-0.md` §11 + §0/§3.3/§4.4/§8 改訂。CEO 監査 + 内部敵対批評（4 レンズ・workflow は rate-limit 失敗 → 自己適用）。①論理修正: 「入力が安全だから文面も安全」は言い過ぎ → 「**consumer view が安全ゆえ入力面は安全。出力文面の安全性は exact template 正本 + walker で保証**」。②**exact template catalog 正本化**（§11.1-3）: claim 4（observation=「ひとつ、メモしておきます。」/ status_note=「ひとつ、心に留めておくとよさそうです。」[不安煽り回避] / info_incomplete / needs_confirmation）・question 3（needs_verification=「確認したいことがあります。」[強すぎ回避] / resolve_overlap=「重なって見える予定があります。」[見える=衝突/重複断定なし・RJ1b] / resolve_missing_info=「伺いたいことがあるかもしれません。」[補う=曖昧回避]）。③**exact choice label 正本化**: needs_verification=[はい/いいえ/あとで]・**resolve_overlap=[あとで確認/まだ決めない]**（候補A/B も衝突 presuppose ゆえ不可・最小 defer のみ）・resolve_missing_info=[あとで確認/そのまま]。④**forbidden lexicon**（verdict/delay-departure-route/sensitive-work-reservation-otherPeople/probability/action/raw id token/hallucination）+ **allowed lexicon** 確定。⑤copyViolations を **whitelist 方式**精密化（exact template/choice label whitelist + forbidden scan + serialization backstop + view precheck + dynamic interpolation 禁止 + LLM 禁止）。⑥`renderCopy` は入力 view を `surfaceProjectionConsumerViewViolations` で**再検証**し違反なら throw（RJ2d 信用しきらない）。⑦実装 GO に **CEO 文面承認 gate**（exact template/choice label/tone/世界観・RJ2f とは別 gate）。自己判定: RJ2e 技術設計 ready（exact catalog 正本化済）だが実装 GO は CEO 二重 gate（技術 + 文面承認）待ち。code なし。
- **RJ2e（CopySurface 実装）= consumer view → user-facing 文面（初の自然言語）**（`5d8edffb`）。新規 `lib/plan/realityCore/copySurface.ts` + `tests/unit/copySurface.test.ts`（**RJ2a/b/c/d 4 ファイル不接触**・consumer view のみ consume・純 additive）。型: `RenderedCopyV0` / `RenderedClaimCopy` / `RenderedQuestionCopy` / `CopyTone`。関数: `renderCopy`(consumer view→exact 文面・**view precheck で冒頭再検証 throw**) / `copyViolations`(exact whitelist + forbidden lexicon + serialization backstop)。**CEO 文面承認済 exact catalog**: claim 4(メモがあります。/ 確認前の注意点があります。/ まだ未確定の点があります。/ 確認が必要な点があります。)・question 3(確認しますか？/ 重なって見える予定があります。確認しますか？/ 未確定の点を確認しますか？)・choiceLabels(needs_verification=[確認する,あとで]/ resolve_overlap=[あとで確認,まだ決めない]/ resolve_missing_info=[確認する,そのまま])。三層防御: ①入力 view precheck(surfaceProjectionConsumerViewViolations 違反で throw・RJ2d 信用しきらない)・②出力 exact catalog 固定 lookup(**dynamic interpolation なし・LLM 自由生成なし**)・③copyViolations walker(exact template/choice label whitelist + forbidden lexicon scan[verdict/delay-departure/sensitive-work-reservation/probability/action/raw id] + serialization backstop + forbidden field)。resolve_overlap は「見える」で衝突/重複断定なし(RJ1b)。**12 tests PASS**・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・next build PASS。RJ2a/b/c/d/既存 6 判断器/ern/cs/mv/snapshot/identity 不接触・UI/storage/API/DB/location/notification/external read 不接触・tree clean・production gate 未通過。CEO 文面承認 gate 通過済(exact catalog)。RJ2f(notification) HOLD 維持。次は **RJ2f 実装に進まず停止** → RJ2f-0 Notification / Contact Boundary Design を docs-only 提出。
- **RJ2f-0A（Channel Type / Pull Surface Semantics closeout・docs-only）= 型と裁定の矛盾補正**（`d3e53c93`）。設計書 `…rj2f-0.md` §11 + §0/§2/§3/§5/§9/§10 改訂。CEO 監査: 本文で「push/chat/external は型にも経路にも持たせない」と裁定しながら `DeliveryChannel` union に含めていた矛盾。①**`DeliveryChannelV0 = "none" | "in_app_passive"` に閉じる**（push/chat/external を runtime type から除外・型レベル経路遮断・future backlog は docs のみ・型にしない）。②**`in_app_passive_eligible` の意味論を強く固定**: pull surface(user がアプリを開いた時に表示してよい候補)であって push/notification/chat/external/dispatch instruction でない・deliveredNow=false と常に両立・notification permission と別。③`deliveryModeCeiling` を既存 enum(`interventionDecision.ts:48` = none/passive_surface/active_prompt)と照合・active_prompt を delivery command にしない(INV-11)・**unknown/unsupported ceiling は conservative に no_delivery**(passive_surface 一致時のみ eligible)。④opt-in を **`userNotificationOptIn` → `userInAppSurfaceOptIn` にリネーム**(案 A・notification opt-in と in-app passive 表示を分離・将来 push 解放時の権限境界混在を防ぐ)・`requiresUserOptIn` → `requiresInAppSurfaceOptIn`。⑤`deliveredNow=false` 不変条件 + v0 で push/chat/external/notification/contact/dispatch/sendNow/recipient/payload/url/token が型にも経路にも存在しない確認(push SDK 不配線)。自己判定: RJ2f 技術設計 ready(補正後・型を v0 用に閉じ pull surface 意味論固定)だが実装 GO は CEO 三重 gate(技術 + 通知方針承認 + production)待ち。code なし。
- **RJ2f（DeliveryGate 実装）= 配信「可否」境界・v0 は配信しない**（`37d76b2f`）。新規 `lib/plan/realityCore/deliveryGate.ts` + `tests/unit/deliveryGate.test.ts`（**RJ2a–2e 5 ファイル不接触**・既存 6 判断器不接触・consume only・純 additive）。型: `DeliveryDecisionV0` / `DeliveryChannelV0`(none/in_app_passive) / `DeliveryEligibilityV0`(no_delivery/in_app_passive_eligible) / `DeliveryGateInput` / `DeliveryGateTrace`。関数: `evaluateDeliveryEligibility`(decision + opt-in/fatigue→**v0 常に no_delivery 寄り**) / `deliveryGateViolations`。**RJ2 surface chain の終端**。**CEO 4 追加ガード実装**: ①**forbidden identifier source-scan**(配信系語 push/chat/external/notification/dispatch/send/recipient/payload/token/url/webhook/email を実装本体に残さない・walker は allowlist key 方式で未知 field を弾き・配列は spread 構築で `push` 呼び出しを避ける)・②**deliveredNow は入力非依存のリテラル false**(条件分岐なし・kill-switch)・③**in_app_passive_eligible は全条件 AND**(ask_clarification ∧ passive_surface ∧ optIn ∧ budget>0)・④**active_prompt は常に no_delivery**(将来上限であって配信命令でない・INV-11)。silent/observe/internal_prepare/blocked → no_delivery。unknown ceiling conservative。in_app_passive = pull surface(通知でない・deliveredNow=false 両立)。push/chat/external は型にも経路にも存在しない。**14 tests PASS**(forbidden identifier source-scan 含む)・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・next build PASS。RJ2a–2e/既存 6 判断器/ern/cs/mv/snapshot/identity 不接触・UI/storage/API/DB/location/notification/external read/push SDK 不接触・tree clean・production gate 未通過。**実配信は CEO + production 三重 gate 待ち**。次は **production/deploy/push/notification 解放に進まず** RJ2-CLOSEOUT Reality Surface Chain Integration Audit を docs-only 提出。
- **RJ2g（Internal Dogfood Preview 実装）= RJ2 chain 出力を operator が観測する read-only 面（初の UI 接続）**（`e8a02032`）。新規 `lib/plan/realityCore/dogfoodPreview.ts`（safe payload 構築 + token leak guard・pure）+ `app/(culcept)/plan/dev-reality-surface/page.tsx`（server・三重ガード+flag+auth）+ `RealitySurfaceDogfoodClient.tsx`（client・表示専用）+ tests 2 + `lib/plan/featureFlags.ts` に `realitySurfacePreview`(env REALITY_SURFACE_PREVIEW・server-only・default OFF) 追加。**設計判断**: RC2a は実データ未配線（zero non-test consumer）ゆえ first UI と first 実データ配線を分離（CEO ③④）→ **v0 は代表シナリオ（DB read なし・決定論的 fixture・observe/ask/overlap/silent）**。read-only は DB 不接触で構造的保証。実 operator anchor 配線は別 GO。**三重ガード**(既存 `isCandidateActionsPreviewHostAllowed`: host opt-in + staging ref + production deny → notFound)+ flag default OFF + operator auth(supabaseServer・owner-RLS・service_role 不使用)。**client props 専用 safe DTO** `RealitySurfaceDogfoodPreviewPayloadV0`(consumerView[RJ2d]/renderedCopy[RJ2e]/delivery safe subset[eligibility/channelCeiling/deliveredNow]のみ・internal object/trace/id/suppressedReasons/carriedDecisionKind 不渡し)。**token leak guard**(payload JSON に ern:/cl:/q:/sp:/pj:/snapshot/evidence/trace/gate/derivedFrom/why/sensitive/reservation/work/otherPeople/confirmed/inferred/graphViewerKey 非出現・fail-closed)。各シナリオは RJ2d/RJ2e walker 再実行で safe 確認。client は型のみ import(derive 関数なし)・no onClick/action/fetch/supabase/localStorage・choice は span(押せない)。**deliveredNow=false 維持**。disabled path(flag OFF/host NG/非 operator)は chain 非実行。**22 tests PASS**(dogfoodPreview 9 + page source-scan 4 + 既存 surface 101 のうち)・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・**next build PASS(/plan/dev-reality-surface dynamic route 生成確認)**。6 surface module 不接触・DB write 0・API route 追加 0・notification 0・push SDK 0・OFF 時 product UI 不変(新規隔離 route)・tree clean・production gate 未通過。次は **production/deploy/push/notification/broad UI 接続に進まず** RJ2h-0 Dogfood Acceptance / Preview Smoke Plan を docs-only 提出。
- **RD1a（Operator Day Real-Data Preview Wiring 実装）= 初の実 operator-data 読み取り（one-off 当日のみ・read-only）**（`9672b207`）。新規 `lib/plan/realityCore/operatorDayPreview.ts`（read-only orchestration + safe DTO + leak guard）+ `tests/unit/operatorDayPreview.test.ts`。変更: `app/(culcept)/plan/dev-reality-surface/page.tsx`（real-data section 追加・gate+auth の後で listAnchors read）+ `RealitySurfaceDogfoodClient.tsx`（real section・fixture と明確分離）+ page test。**CEO RD1a GO（one-off のみ・recurring 除外・no-fallback・初の実データ・2026-06-14）**。**独立裁定（GPT 鵜呑みにしない）**: field adapter は不要だが orchestration adapter は必要（RD0 で確定）。**RC2a 初の実データ配線**ゆえ blast radius を **one-off 当日 anchor のみ**に限定（recurring は読む+件数把握するが当日 graph に入れない=展開しない・RD1b）。orchestration: `selectTodayOneOff`(filter)/`buildOperatorDaySnapshot`(pure・既存 honest compile consume)/`buildOperatorDayRealPayload`(async・listAnchors 注入・owner-RLS)/`realDayPayloadLeakViolations`。**fake 禁止確認**: real anchor→RC2a で place→unknown / leaveBy→null / ETA/route→knownFalse / companions 無→otherPeople unknown を test #8-10 で snapshot に直接 assert。safe DTO `RealDaySurfacePayloadV0`(mode/available/reasonCode generic/summary{count}/consumerView/renderedCopy/delivery safe subset)。**raw anchor/internal/recurrenceRule/companions/sourceId/externalUid/title を client に渡さない**(leak guard 24 token・fail-closed)。**unavailable 時 fixture へ fallback しない**(null content + reasonCode)。subjectiveDate は server now(JST v0)。deliveredNow=false 維持。page は flag+auth の後で real read・leak guard double-guard。**敵対的 red-team(ultracode workflow `wf_3563c2f5`・4 レンズ・PII/fallback/write/fake)= 0 real defect**(safe DTO/recurring 除外/fail-closed/read-only/deliveredNow false 全確認)。**31 tests PASS**(operatorDayPreview 9 + page 9 + dogfood 9 + 既存)・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外・PlanClient は並列 flaky・単独 PASS)・**next build PASS**(/plan/dev-reality-surface)。RJ2a–2g 6 surface module 不接触・**DB write 0・service_role 0・API route 追加 0・notification 0・localStorage 0・external read 0**(listAnchors select のみ)・OFF 時 product UI 不変・tree clean・production gate 未通過。次は **recurring expansion/Alter tab/本線/production/notification に進まず** RD1b-0 Recurring Expansion / Real Data Coverage Design を docs-only 提出。
- **RD1b（Recurring Expansion 実装）= operator 当日 recurring を既存展開器で安全に追加（read-only）**（`109ccee2`）。新規 `lib/plan/recurringDayResolver.ts`（`resolveTodayRecurring`・既存 `expandRecurrence` consume）+ `tests/unit/recurringDayResolver.test.ts`。変更: `operatorDayPreview.ts`（recurring 当日展開を統合・summary 4 count 化）+ test + client（4 count 表示）+ page test。**CEO RD1b GO（既存 expander consume・便利に補完しない・2026-06-14）**。**独立裁定**: production `anchorsForDay`（`countOccurrences(a,day,day)>0`）が **pass-through**（materialize しない・recurring kind のまま返す）と確認 → 同契約。`resolveTodayRecurring` は **既存 `expandRecurrence`（WEEKLY RRULE・exceptionDates/validFrom/validUntil 厳密適用・不正→空）に委譲**・**新規 RRULE parser を書かない**。当日 occur recurring を **AS-IS で当日 graph に入れ**（eventNodes が展開済として処理）、**不正/非WEEKLY/期限外は invalidCount に計上し当日に入れない**（過少 > 捏造）・valid だが当日でないは excludedCount。date util ゆえ `new Date(constant)` を recurringDayResolver に隔離（IO scan は Date.now/Math.random/fetch のみ・new Date 許容）。**duplicate 断定しない**: one-off + recurring 同 timeWindow → RJ1b `exact_time_collision_ambiguous`（confirmed/infeasible 直行しない・test #8/#9 で assert）。safe DTO summary = oneOffIncludedCount/recurringIncludedCount/recurringExcludedCount/recurringInvalidCount（count のみ）・**raw recurrenceRule/exceptionDates/sourceId/externalUid/companions/title を client に渡さない**（leak guard 維持）・fixture/real 分離・no-fallback・deliveredNow=false。**36 tests PASS**(recurringDayResolver 8 + operatorDayPreview 11 + page 9 + dogfood 9 のうち)・tsc baseline 55 維持(additive・0 error)・full suite FAIL 2 のみ(realityCore 外)・**next build PASS**(/plan/dev-reality-surface)。RJ2a–2g 6 surface module 不接触・recurrence-expander 不接触(consume のみ)・DB write 0・service_role 0・API route 0・notification 0・localStorage 0・OFF 時 product UI 不変・tree clean・production gate 未通過。次は **Alter tab/本線/production/notification に進まず**（当該セッション新アイデアの実装完了まで production 不可）RD1c-0 Real Data Dogfood Smoke / Coverage Gap Review を docs-only 提出。
- **RD1c-0（Real Data Dogfood Smoke / Coverage Gap Review）= docs-only**（`ec9ec7c4`）。`docs/reality-real-data-dogfood-coverage-rd1c-0.md`。real-data preview で **何が unknown になるか**を RC2a 実装読み取りで実測（place/route/ETA/leaveBy/movement = 全 unknown/knownFalse・確定は時刻/rigidity/sensitive/companions gate のみ）。「薄さ」を bug でなく coverage gap として可視化。**次方向（Mobility/Place 供給 vs Alter tab 接続）を CEO 判断する基準**を提示。コードなし。
- **RD2-0（Mobility / Place Supply Design）= docs-only**（`39fb0144`）。`docs/reality-mobility-place-supply-rd2-0.md`。並列 codebase 監査（workflow `wf_6cc73cbf`・4 subsystem）で **供給インフラ大半が既存・RC2a 未配線**と確定（placeResolver/transport L-1L-2 cascade/currentLocationGating/JMA 実装済）。place 6 段・origin 5 段・route/ETA 6 段・leaveBy 全条件 AND・fake 禁止境界・RC2a 接続（unknown→inferred adapter・honest 構造維持）・currentLocation/external API gate・dogfood 反映・RD2a-e 候補を設計。**§2.1 CEO 補正反映**: 整形状態 ≠ 確認・exact_confirmed は確認 provenance のみ。コードなし。
- **RD2a（PlaceResolution schema/types only 実装）= 場所解決の段階・不変条件を pure 型化**（`0b7e8f90`）。新規 `lib/plan/realityCore/placeResolution.ts`（import ゼロ・pure）+ `tests/unit/placeResolution.test.ts`。**CEO RD2a GO（types only・provider/API 接続なし・2026-06-14）**。**CEO 補正核心（整形状態 ≠ 確認）を型レベル強制**: `createExactConfirmedResolution` は `ConfirmedPlaceSource`（user_selected/user_confirmed/persisted_selected/trusted_exact_source）のみ受理し、location_text/places_api_candidate/canonical_text からは **TS 上 confirmed 構築不能**。型: `PlaceResolutionV0`/`PlaceResolutionStage`(6)/`PlaceCertaintyStatus`/`PlaceResolutionConfidence`/`PlaceResolutionSource`/`ConfirmedPlaceSource`/`PlaceCandidateRef`(opaque・raw placeId/lat/lng なし)/`PlaceResolutionEvidenceRef`/`PlaceResolutionMissingInput`/`PlaceResolutionDisplayPolicy`。constructor 6 + `placeResolutionViolations`(walker)。**不変条件（walker 強制）**: stage→certaintyStatus 固定（missing/location_text_only/candidate_unresolved/ambiguous=unknown・candidate_selected=inferred・exact_confirmed=confirmed）・confirmed は確認 provenance + 非空 evidence + confidence + evidence sourceKind も確認 provenance・ambiguous は候補 ≥2・raw field(lat/lng/placeId/locationText/address/coordinates) と mobility field(route/eta/leaveBy/movementRequired) を構造 backstop で検出（この型に route/ETA/leaveBy/movement を持たない=場所解決の責務外）。`out.push(` 回避で walker は concat ベース（forbidden identifier scan 対策・operatorDayPreview と同方針）。**23/23 tests PASS**（CEO 必須 14 項目: missing=unknown / locationText only≠confirmed / Places 候補のみ≠confirmed / municipality・prefecture 座標≠confirmed / canonical text≠confirmed / candidate_unresolved≠confirmed / ambiguous≠confirmed / candidate_selected=inferred 止まり / exact_confirmed=explicit confirmation evidence 必須 / evidence・source・confidence 欠落→violation / raw lat/lng/placeId/locationText 非露出 / route・ETA・leaveBy field なし / currentLocation・external import なし / IO source-scan green）。**tsc 0 placeResolution errors・total 55（baseline 不変・additive）**。build は環境 OOM（system free RAM ~67MB・compile-start で OOMError・**import ゼロの additive 型ゆえ production route から未参照=build 出力に影響なし**）。**placeResolver import なし・Places/geocode/currentLocation/external 接続なし・RC2a 既存 compile/MovementReality 不接触**・UI/storage/API route/DB write/Supabase/notification/external read 不接触・production gate 未通過・tree clean。次は **RD2b 実装に進まず** RD2b-0 Place Candidate Adapter Design を docs-only 提出。
- **RD2b（Place Candidate Adapter pure implementation）= provenance → stage 写像 adapter**（`e1e429a3`）。新規 `lib/plan/realityCore/placeCandidateAdapter.ts`（pure・provider 引数注入）+ `tests/unit/placeCandidateAdapter.test.ts`。**CEO RD2b GO（pure adapter のみ・provider 実行/Places API/geocode/currentLocation/RC2a 接続なし・2026-06-14）**。**思想（adapter は確信度を上げない）**: `resolvePlaceCandidate(input, deps)` が locationText / provider result / confirmation event を `PlaceResolutionV0` に写像。優先順位 **confirmation（確認 provenance・exact_confirmed）> selection（candidate_selected・inferred）> [locationText 有無] > provider 候補（candidate_unresolved / ambiguous_place・unknown）**。**resolver confidence high でも candidate 止まり**（confidence ≠ 本人確認）。**純粋性（RD1a listAnchors と同型）**: provider は引数注入（adapter は placeResolver を import しない・Places API を叩かない）・**provider 未注入 → location_text_only**・**provider 失敗 → location_text_only（fake 候補を作らない）**・adapter は async だが await は注入 provider のみ。**raw 不露出**: adapter は raw lat/lng/placeId を**受け取らない**（provider は opaque ref のみ返す契約）・`placeAdapterOutputViolations` が構造 + serialization token + 座標パターンを backstop。型: `PlaceProviderCandidateResult`/`PlaceCandidateProvider`/`PlaceSelectionInput`/`PlaceConfirmationInput`(source は ConfirmedPlaceSource 型制約)/`PlaceAdapterInput`/`PlaceAdapterDeps`。**19/19 tests PASS**（CEO 必須 17 項目: missing/text_only/provider 未注入/候補0/候補1/拮抗/canonical selected=inferred/confidence high でも confirmed でない/confirmation のみ confirmed/non-confirmed source から confirmed 不可/provider failure で fake なし/raw 非露出/candidateRef opaque/mobility field なし/import scan/IO scan/全 stage green）・RD2a+RD2b 併走 42/42 PASS。**tsc: RD2b source+test 0 error（with/without isolation で 60/60・寄与 0）**。total 60。**※ baseline 55→60 の +5 は `tests/unit/stargazer/conversationQualityAudit.test.ts` の FactTag "energy_source" drift（stargazer test・place/mobility と無関係・RD2a/RD2b いずれの commit でも未 touch・別途 flag）**。**build: 8GB RAM 環境 OOM（exit 134・Ineffective mark-compacts）**。CEO 承認の **pure-type slice 環境例外**を適用（import は realityCore 内 pure module のみ・production route 未参照・UI 未接続・runtime 影響なし・targeted tests + tsc を受領根拠）。BX0 候補として継続記録。**placeResolver/geocode/currentLocation/external/DB/Supabase/localStorage/UI/Alter tab/本線/RC2a compile 不接触・route/ETA/leaveBy/movementRequired 生成なし**・production gate 未通過・tree clean。次は **RD2b' (Places consume)/RC2a 接続/永続化に進まず CEO 判断待ち**（BX0 build 環境対策は別 slice）。
- **TSC-B0（FactTag Baseline Hygiene micro-fix）= tsc baseline 正常化の最小修正**（`48729054`）。`tests/unit/stargazer/conversationQualityAudit.test.ts` の mockFacts が `tags:["energy_source"]` を使用していたが `FactTag` union（`alterHomeAdapter.ts:844`）に該当値なし（有効値 `"energy_state"`）→ TS2322 ×5。**事実確認**: `"energy_source"` は全リポジトリで当 test の 5 箇所のみ（runtime/source 不在）= test-only typo・`"energy_state"` は有効 FactTag で他 11 箇所実使用・当 test は turnNumber rotation（tag 意味論非依存）→ runtime 不変。**採用**: test fixture correction `"energy_source"`→`"energy_state"` ×5（union 拡張せず・FactTag 意味拡張なし）。**conversationQualityAudit 146/146 PASS・tsc total 60→55（baseline 完全復元）**・test-only・production source 不接触。RD2b で混入した unrelated drift（RD2a/RD2b 起因でない）を CEO 指示で次実装前に正常化。
- **RD2c（OriginInference schema/types only 実装）= origin 推定を「由来と信頼度を持つ推定段階」として pure 型化**（`d5a90500`）。新規 `lib/plan/realityCore/originInference.ts`（import ゼロ・pure）+ `tests/unit/originInference.test.ts`。**CEO RD2c GO（types only・currentLocation 取得/geolocation/route-ETA-leaveBy/RC2a 接続なし・2026-06-14）**。**前提を疑う（place §2.1 と同型）**:「現在地」が最強シグナルでも **confirmed にしない** —「今いる場所」≠「出発する場所」（出発前に移動しうる）。confirmed は **user_confirmed_origin（本人確認）のみ**・previous_event_end/home_assumed/work_assumed/current_location_candidate は全て **inferred 止まり**。**革新的安全則**: **confidence の `high` は confirmed に予約**（inferred は `moderate` 上限・gated GPS でも candidate）— reality OS は本人未確認の origin に「高確信」と言わない。型: `OriginInferenceV0`/`OriginInferenceStage`(6)/`OriginCertaintyStatus`/`OriginInferenceConfidence`/`InferredOriginConfidence`/`OriginInferenceSource`/`ConfirmedOriginSource`(user_confirmed のみ)/`OriginRef`(opaque)/`OriginEvidenceRef`/`OriginMissingInput`/`OriginDisplayPolicy`。constructor 6 + `originInferenceViolations`(walker)。**不変条件（walker 強制）**: stage→certaintyStatus 固定・confirmed は user_confirmed + 非空 evidence + high + evidence sourceKind も確認・**current_location_candidate は gate 済み（gated_current_location）evidence 必須**（RD2c は gate を実行せず結果のみ受け取る = RD2b provider 注入と同型・navigator/geolocation 不接触）・unknown_origin は source none で **home/currentLocation auto-fill 禁止**・raw(lat/lng/address/label) と mobility(route/eta/leaveBy/movementRequired) を構造 backstop で検出。**20/20 tests PASS**（CEO 必須 13 項目）・RD2a+RD2b+RD2c 併走 **62/62 PASS**。**tsc 0 originInference errors・total 55（TSC-B0 baseline 維持）**。**※ source-scan が違反メッセージ文字列内の camelCase `"currentLocation"` を検出 → `"current_location"` に修正（防御が機能）**。build は 8GB RAM 環境 OOM（pure-type 環境例外・production route 未参照・UI 未接続）。**currentLocation 取得/geolocation/locationResolver/external/DB/Supabase/localStorage/UI/Alter tab/本線/RC2a compile/PlaceResolution/PlaceCandidateAdapter 不接触・route/ETA/leaveBy/movementRequired 生成なし**・production gate 未通過・tree clean。次は **route/ETA/leaveBy/currentLocation 実装に進まず** RD2d-0 Route / ETA Supply Boundary Design を docs-only 提出。
- **RD2d-0A（Route / ETA Supply Boundary 精緻化）= docs-only**（`6656f0bf`）。`docs/reality-route-eta-supply-boundary-rd2d-0a.md` + RD2d-0 §9 補正。**GPT 監査 7 点を独立検証 → 全採用 + GPT を超える 3 追加**（CEO ⑥⑦）: ①**単調 capability lattice**（transportModeKnown→routeShapeKnown→travelDurationKnown→trafficAware→temporalScopeKnown→arrivalTargetKnown→etaKnown→leaveByEligible・各 rung が下位を含意必須）②**per-capability provenance**（各能力ビットが独立 source/evidence）③**traffic-aware ETA でも inferred**（予測であって確定でない・confirmed は user 確認 in-scope のみ）。確定項目: route/duration/ETA/leaveBy 分離（external API が duration 返すだけで etaKnown/leaveBy にしない）・**identity（routeEtaSupplyId = full basis content hash・origin/dest/mode/time/targetNode/subjectiveDate/providerKind/providerVersion・from-to text/raw lat-lng/array index 禁止・mode 不明で confirmed 禁止）**・**freshness/invalidation（fetchedAt/validUntil/freshnessStatus/staleReason/invalidationTriggers・stale なら etaKnown false/leaveBy 不可）**・**heuristic 境界（internalReference|debugOnly・etaKnown=false・leaveBy/feasibility/proposal/copy 禁止・「○分」断定禁止）**・**user_confirmed_route scope（confirmedScope 一致 + recency・別 origin/dest/mode 流用禁止）**・**origin temporal usability（current_location_candidate は future departure で leaveBy origin 不可 — 出発時刻に移動しうる・originUsabilityForRouting vs forLeaveBy 分離）**・**endpoint pair gate（origin/destination のペアで外部送信判定・片側 sensitive/home/work/currentLocation で block）**・RC2a 接続条件（heuristic で消してはいけない missingInputRefs）。**RD2d 実装 GO 可（CEO 承認後・lattice 型 + heuristic 隔離 + identity/freshness 型 + pure adapter まで・external/cascade 接続/RC2a 接続/永続化は各々別 GO + gate）**。コードなし。
- **RD2d-0B（Route / ETA Mode-Aware Capability Graph）= docs-only**（`f8969e4d`）。`docs/reality-route-eta-supply-boundary-rd2d-0b.md` + RD2d-0A §1 補正（単調 lattice 撤回）。**GPT 監査 8 点を独立検証 → 採用 + 独立強化**: RD2d-0A の**単調 lattice は設計ミス**（`trafficAware` を全 mode 共通必須にすると徒歩/鉄道を不当に unknown へ落とす・arrivalTarget[event 側]/leaveBy[join 結果]を transport lattice に混入）→ **mode-aware capability DAG** に修正。確定: ①capability DAG（ROUTE/DURATION/TEMPORAL/CONDITION/PROJECTION/PLANNING/ELIGIBILITY ファミリ + precise edges・各能力は edges 入力以上を主張しない monotone 安全性のみ継承・routeShape と duration は独立）②**mode×condition adequacy**（car=traffic_aware / walking=static_assumption可・traffic=N/A / transit=schedule_aware / unknown mode=projection 不可）③**duration≠arrivalProjection≠timeEstimateUsableForPlanning≠leaveByEligible**（旧 etaKnown 廃語・external が duration 返しても travelDuration 止まり）④user_confirmed_route を confirmedScope 別に分解（全能力 true にしない・流用禁止）⑤**origin conflict handling**（originConflictStatus/Policy・user_confirmed origin を currentLocation で上書きしない・conflict で leaveBy 不可）⑥**cache 分離**（routeShape/duration/trafficEta/scheduleEta/userConfirmedDuration・種別別 stale）⑦heuristicDurationHint 使用境界（internalReference/debugOnly・action input 禁止・許容は debug/coverage/calibration/burden ref のみ）⑧endpoint pair privacy（pairExternalSendAllowed・片側 sensitive で送信 block・raw 座標 log/id 禁止・currentLocation 最強 sensitive・local heuristic は別判断）。独立強化: precise edges DAG / mode×condition adequacy マトリクス / leaveBy=明示 join。**RD2d 実装 GO 可（CEO 承認後・capability graph 型 + mode×condition + heuristic 隔離 + identity/freshness 型 + pure adapter まで・external/cascade/RC2a 接続/永続化/conflict 解決/cache 実装は各々別 GO + gate）**。コードなし。
- **RD2d-a（Route/ETA mode-aware capability DAG・types + walker 実装）= pure 型 + invariant + walker**（`40c0146f`）。新規 `lib/plan/realityCore/routeEtaCapability.ts`（import ゼロ・pure）+ `tests/unit/routeEtaCapability.test.ts`。**CEO RD2d-a GO（types + walker only・adapter/provider 接続なし・2026-06-14）**。RD2d-0B の DAG を型化。型: `RouteEtaCapabilityV0`/`TransportModeV0`/`RouteCapabilityV0`/`DurationCapabilityV0`/`DurationBasisV0`/`TemporalCapabilityV0`/`ConditionCapabilityV0`/`ConditionModelStatusV0`/`PlanningEligibilityV0`/`LeaveByEligibilityPreconditionV0`/`RouteEtaIdentityBasisV0`/`RouteEtaFreshnessV0`/`RouteEtaEvidenceRef`/`RouteEtaMissingInput`/`EndpointPairPrivacyGateV0`/`OriginConflictForRouteV0`。関数: `conditionAdequateForMode`/`deriveCapabilityFlagsFromParts`/`deriveEndpointPairGate`/`routeEtaCapabilityViolations`/`endpointPairPrivacyViolations`/`buildRouteEtaCapability`。**不変条件（walker 強制）**: DAG derive 整合（travelDuration≠arrivalProjection≠timeEstimateUsableForPlanning≠leaveByEligible・各能力は入力以上を主張しない）・mode×condition adequacy（car=traffic_aware/walk・bike=static|weather|na/transit=schedule_aware/unknown=projection 不可・**trafficAware 共通必須にしない**）・**routeShape と travelDuration 独立**・heuristic は travelDuration 止まり（projection/planning/leaveBy 不可・displayPolicy internalReference|debugOnly）・stale→planning false・origin conflict→leaveBy false・current 観測で user_confirmed origin 上書き禁止・endpoint pair（片側 sensitive/current 観測/home-work で external send 不可・current 観測座標最強 sensitive・raw 座標 log/id 禁止）・confidence high は user_confirmed in-scope 予約・raw polyline/座標 serialization backstop。**自己補正**: RD2d-0B §1.2 の leaveBy join から pairPermissionOk を除去（endpoint gate は external send 可否を govern・leaveBy 条件でない）。**22/22 tests PASS**（CEO 必須 21 項目）・RD2a+RD2b+RD2c+RD2d-a 併走 **84/84 PASS**。**tsc 0 routeEtaCapability errors・total 55（TSC-B0 baseline 維持）**。※ FORBIDDEN_RAW_TOKENS の "rawcoord" が legit field 名に部分一致→除去（coordinates 等維持）。build は 8GB RAM 環境 OOM（pure-type 環境例外）。transport provider/cascade/Google Routes/geocode/currentLocation/weather/RC2a compile/UI/DB/Supabase/localStorage/notification/external 不接触・**adapter 未実装**・production gate 未通過・tree clean。次は **RD2d-b 実装に進まず** RD2d-b0 Provider Adapter Design を docs-only 提出。
- **RD2d-b0（Route/ETA Provider Adapter Design）= docs-only**（`5d3b6f51`）。`docs/reality-route-eta-provider-adapter-rd2d-b0.md`。provider 出力 → capability graph 写像 adapter の設計。核心: **adapter は能力を上げない**（必ず `deriveCapabilityFlagsFromParts`[RD2d-a DAG]経由）。確定: provider injection（pure・RD2b 同型・provider は opaque ref のみ返し raw 座標を adapter が見ない）・既存 transport cascade/heuristicDistanceProvider/unresolvedProvider/manualUserProvider を RD2d-b 実装で provider ラッパ注入（opaque 圧縮）・heuristic→durationBasis heuristic→projection/leaveBy 不可（internalReference 隔離）・stage mapping（no_route/heuristic/scheduled/user_confirmed/cached/external→必ず DAG 経由）・provider failure で fake route/duration 作らない・raw route data は provider ラッパ内部完結・RC2a 接続条件（routeKnown/etaKnown/leaveByKnown・heuristic で消してはいけない missingInputRefs）・leaveBy へ渡すのは leaveByEligible のみ（endpoint pair gate は leaveBy 条件でない）。RD2d-b 候補: pure adapter→cascade consume→RD2e leaveBy→RC2a 接続→external（pair gate・最後）。コードなし。
- **RD2d-a-A（Route/ETA capability 語彙・projection allowlist・leak guard micro-fix）= pure 補正**（`2faf8a2d`）。`lib/plan/realityCore/routeEtaCapability.ts` + test + RD2d-0B doc 補正。**CEO「自立して練り直せ」を受け 4 レンズ敵対監査（ultracode workflow `wf_cef6e0fa`: naming/DAG/leak/leaveBy）を回し、GPT 指摘 + 監査が見つけた 2 つの追加 hole を独立検証して反映**。①**語彙**: `travelDurationKnown`→`durationSignalPresent`（heuristic でも true ＝「known」は誇張・durationBasis が真の epistemic 状態を持つ）/`durationScopeKnown`→`durationScopeBounded`/`temporalFreshnessKnown`→`temporalFreshnessEvaluated`（real freshness と区別）。他の -Known は honest binary ゆえ維持。②**projection DENYLIST→ALLOWLIST（DAG 監査が見つけた fail-open hole・GPT 指摘外）**: 旧 `!heuristic` denylist は straight-line を external_route と誤 stamp / basis="none" でも projection に登れた → `durationProjectionGradeOk(∈{scheduled,user_confirmed,external_route,cached_route})` ALLOWLIST（fail-closed）を arrivalProjection の独立 conjunct に。conditionAdequateForMode と直交（条件は condition・duration 品質は basis grade）・walking straight-line は condition adequate でも duration grade で落ちる。`durationScopeBounded` も projection conjunct に追加。walker に明示 invariant も追加。③**leaveBy 語彙**: `leaveByEligible`→`leaveByComputable`（tier-1 内部計算可能性のみ・Eligible は本 codebase で display/permission の carry-word ゆえ誤読危険）・`LeaveByEligibilityPreconditionV0`→`LeaveByComputabilityV0`・INV computable ⇏ display ⇏ departure-line ⇏ action・**endpoint pair gate は leaveBy 条件でない**（RD2d-0B doc の ∧pairPermissionOk/∧permission を撤回し code と整合）。④**leak guard**: serialized-output scan 維持・token に encodedpolyline/waypoints 追加・**粗い座標 pair pattern**（2 桁小数 lat,lng pair）で COORD_PATTERN（4 桁小数）の blind spot 閉鎖・bare lat/lng は free-text 誤検出ゆえ非採用。**23/23 tests PASS**（CEO 必須 12 + DAG hole(none fail-closed/scope bounded) + 既存 rename）・RD2a+RD2b+RD2c+RD2d-a 併走 **85/85 PASS**・**tsc 0 routeEtaCapability errors・total 55（baseline 維持）**。build は 8GB RAM 環境 OOM（pure-type 環境例外）。transport provider/cascade/external/currentLocation/weather/RC2a compile/UI/DB/notification 不接触・adapter 未実装・production gate 未通過・tree clean。次は **RD2d-b 実装に進まず** CEO 判断待ち（RD2d-b0 は `5d3b6f51` で提出済）。
- **RD2d-b0A（Route/ETA Provider Adapter Design 語彙同期）= docs-only**（`2767d711`）。`docs/reality-route-eta-provider-adapter-rd2d-b0.md` 補正。RD2d-a-A（`2faf8a2d`）の実装が RD2d-b0 より後だったため doc に旧語彙が残存していた（adapter 実装で旧意味論復活のリスク）→ 同期。①**provider result/mapping 語彙**: `travelDurationPresent`→`durationSignalPresent`（「所要が分かった」でなく signal の有無）・`durationScopeBounded`/`temporalFreshnessEvaluated` 追加・出力 `leaveByEligible`→`leaveByComputable`。②**provider output→capability mapping を fail-closed**: 必ず `deriveCapabilityFlagsFromParts` 経由・direct elevation 禁止・`durationProjectionGradeOk` allowlist 必須・condition adequate だけ/scope bounded なしでは projection 不可・unknown/none/malformed は no_route_source 側。③**heuristic mapping** 厳密化（durationSignalPresent 止まり・projection/planning/leaveBy/action 禁止・displayPolicy internalReference|debugOnly）。④**leaveByComputable 意味論**（内部計算可能性のみ・display/action/departure-line/notification/proposal でない・RJ2/Permission/Delivery gate なしに user-facing 不可）。⑤**provider failure/malformed**: no_route_source へ fail-safe・都合よく補完しない・raw route data を output に載せない・provider overclaim も walker で落とす。⑥**RD2d-b 実装 GO 条件チェックリスト（§9.1）**: 語彙同期済/DAG derive 経由/direct elevation 禁止/allowlist 必須/leaveByComputable 限定/failure fail-safe/heuristic 止まり/route provider・cascade・external 不接触。コードなし。次は **RD2d-b 実装 GO（CEO 判断）待ち**。
- **RD2d-b（Route/ETA Provider Adapter pure implementation）= provider 出力を DAG/walker 経由で写像**（`56b90a06`）。新規 `lib/plan/realityCore/routeEtaProviderAdapter.ts`（pure・provider 注入）+ `tests/unit/routeEtaProviderAdapter.test.ts`。**CEO RD2d-b GO（pure adapter・provider 実行/cascade/external/RC2a 接続なし・2026-06-14）**。**思想（adapter は能力を上げない）**: `resolveRouteEtaCapability(input, deps)` が provider 出力を **必ず `buildRouteEtaCapability`（内部で `deriveCapabilityFlagsFromParts`）+ `routeEtaCapabilityViolations` 経由**で `RouteEtaCapabilityV0` に写像。**direct capability elevation 禁止**（adapter が arrivalProjection/planning/leaveByComputable を直接立てない）。型: `RouteEtaProviderResultV0`/`RouteEtaProviderStatusV0`/`RouteEtaProviderKindV0`/`RouteEtaProviderFailureReasonV0`/`RouteEtaAdapterStageV0`/`RouteEtaAdapterInputV0`/`RouteEtaProvider`/`RouteEtaAdapterDepsV0`/`RouteEtaAdapterOutputV0`。関数: `resolveRouteEtaCapability`/`routeEtaProviderResultViolations`/`routeEtaAdapterOutputViolations`。**fail-closed**: provider 未注入/failure → no_route_source・malformed(enum/raw-leak) → no_route_source・basis none/signal なし → no_route_source・**`durationProjectionGradeOk` allowlist 外(heuristic/none) は projection 不可**・condition だけ/scope なし/temporal 未 scope/stale → projection 不可・provider overclaim も最終 walker で検出(fail loud・漏れる capability を emit しない)。**heuristic は durationSignalPresent 止まり**(displayPolicy internalReference|debugOnly)。**raw 不露出**: provider の opaqueRouteRef を capability に載せない・provider result の raw 座標は `routeEtaProviderResultViolations` が input 境界で検出 → no_route_source。**missingInputs は flag から honest 導出**(heuristic は route_missing/eta_source_missing/leaveBy_missing を消さない)。**21/21 tests PASS**(CEO 必須 25 項目)・RD2a+RD2b+RD2c+RD2d-a+RD2d-b 併走 **106/106 PASS**・**tsc 0 errors・total 55(baseline 維持)**。build は 8GB RAM 環境 OOM(pure adapter 環境例外)。MovementReality/compileMovementReality/RC2a compile/dogfood/UI/Alter tab/本線/cascade/heuristicProvider/external API/currentLocation/weather 不接触・production gate 未通過・tree clean。次は **cascade consume/RC2a 接続/leaveBy/external に進まず** RD2d-c0 Transport Cascade Consume Design を docs-only 提出。
- **RD2d-c0（Transport Cascade Consume Design）= docs-only**（`d17c0938`）。`docs/reality-transport-cascade-consume-rd2d-c0.md`。既存 transport cascade を `RouteEtaProvider` として wrap 注入する設計。核心: **wrapper は翻訳層（能力判定しない）・RD2d-b adapter が DAG で判断**（責務分離）。確定: cascade provider wrapper（`runCascade` consume・RD2d-b deps.provider に注入）・**heuristicDistanceProvider の opaque 圧縮**（durationBasis=heuristic に正直 stamp・raw 座標を opaque ref に圧縮・adapter が capability に載せない二重防御）・provider result normalization（unresolved→no_route/heuristic→signal only/user→user_confirmed・malformed を補完しない・enum 厳密）・privacy guard 尊重（sensitive 座標を扱わない・raw を log/return に出さない）・endpoint pair gate 関係（**pairExternalSendAllowed=false でも heuristic local は別判断で可**・external は別 gate）・**no external route API**（google_routes 触れない・heuristic local まで）・**no RC2a compile change / no leaveBy generation**。RD2d-c 候補: pure wrapper（heuristic local）→ RD2e leaveBy → RC2a 接続 → RD2d-c'（external・最後+gate）。コードなし。
- 以降の各 slice は §2 の RACI 表を完了報告に含める。

### RD1b Department Responsibility Matrix（Recurring Expansion・既存展開器 consume・read-only）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Build**（recurring 展開の technical safety）+ **Product**（dogfood coverage） |
| consultedDepartments | Permission（owner-RLS）・Communication（surface 不変）・Risk（duplicate ambiguity） |
| blockingDepartments | **CEO**（Alter tab/本線/production は別 gate）+ Permission |
| outputs | `recurringDayResolver.ts`（既存 expandRecurrence consume・当日展開 + 4 count）・operator real preview に recurring 統合。**新規 RRULE parser/materialize/notification なし** |
| safetyGate | **既存 tested expander 委譲**（新規 RRULE parser 書かない）・**materialize しない**（recurring AS-IS・anchorsForDay 同契約）・**不正/非WEEKLY/期限外は当日に入れない**（過少 > 捏造・invalidCount 計上）・exceptionDates/validFrom/validUntil 既存適用・**duplicate 断定しない**（RJ1b exact_time_collision_ambiguous）・JST v0・fake 禁止維持・**client safe DTO（count のみ・raw recurrenceRule/sourceId/externalUid/companions/title 不渡し）**・fixture/real 分離・no-fallback・read-only・deliveredNow false・三重ガード+flag OFF+operator auth・**production gate 未通過** |
| traceRefs | expandRecurrence（既存）/ RC2a snapshot（internal）/ 既存 safe DTO（count のみ） |

### RD1a Department Responsibility Matrix（Operator Day Real-Data Preview・one-off のみ・read-only）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Build**（real-data orchestration の技術安全）+ **Product**（dogfood 体験） |
| consultedDepartments | Permission（owner-RLS/auth）・Communication（surface 出力不変）・Risk（unknown 反映） |
| blockingDepartments | **CEO**（recurring/Alter tab/本線/production は別 gate）+ Permission |
| outputs | `operatorDayPreview.ts`（read-only orchestration + safe DTO + leak guard）・`/plan/dev-reality-surface` real section。**recurring 展開/proposal/notification なし** |
| safetyGate | **one-off 当日のみ**（recurring は読む+数えるが当日 graph に入れない）・**read-only**（listAnchors select のみ・owner-RLS・service_role 禁止・write/seed/api/localStorage なし）・**fake 禁止**（place/route/ETA/leaveBy/otherPeople を RC2a 経由で unknown/knownFalse・新規 fake 経路なし）・**unavailable で fixture へ fallback しない**（fail-closed・null content + generic reasonCode）・**client は safe DTO のみ**（raw anchor/internal/recurrenceRule/companions/sourceId/externalUid/title 不渡し）・token leak guard（24 token・double-guard）・subjectiveDate server JST・deliveredNow=false・fixture/real 明確分離・三重ガード+flag OFF+operator auth・**production gate 未通過** |
| traceRefs | listAnchors（owner-RLS）/ RC2a snapshot（internal）/ 既存 safe DTO のみ |

### RJ2g Department Responsibility Matrix（Internal Dogfood Preview・read-only / pull / 配信なし）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Build**（dev preview の技術安全）+ **Product**（dogfood 体験） |
| consultedDepartments | Communication（surface 出力）・Permission（operator auth/露出可否） |
| blockingDepartments | **CEO**（flag 有効化・dogfood 範囲・production は別 gate）+ Permission |
| outputs | `dogfoodPreview.ts`（safe payload + leak guard）・`/plan/dev-reality-surface`（dev-only route・read-only）。**実 operator anchor 配線/proposal/notification なし** |
| safetyGate | **read-only**（v0 は DB read なし・write/seed/apply/notification/push/external/action なし）・**pull-only**（deliveredNow=false 維持・route を開いた時のみ）・**三重ガード + flag default OFF + operator auth**（production 構造的不可視・service_role 禁止）・**client は safe payload DTO のみ**（internal object/trace/id 不渡し・型のみ import・no-action）・**token leak guard**（fail-closed・leak/no-data で Disabled）・各シナリオ RJ2d/RJ2e walker 再実行・OFF 時 product UI 不変（新規隔離 route）・**production gate 未通過** |
| traceRefs | RJ2d consumerView / RJ2e copy / RJ2f delivery safe summary のみ（internal id/trace なし） |

### RJ2f Department Responsibility Matrix（DeliveryGate・配信可否境界・v0 無配信）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Communication**（配信可否境界の技術安全）+ **Ops**（通知運用）+ **CEO**（通知方針） |
| consultedDepartments | Permission（decisionKind/contactPolicy/deliveryModeCeiling）・Product（fatigue/体験）・Growth（トーン） |
| blockingDepartments | **CEO**（通知方針・一斉通知・法務）+ **Permission** + **production gate** |
| outputs | `DeliveryDecisionV0`（配信可否・v0 無配信）・`deliveryGateViolations`。**実配信/push/notification/external なし** |
| safetyGate | **v0 無配信（deliveredNow=入力非依存リテラル false・kill-switch）**・**DeliveryChannelV0 を none/in_app_passive に閉じる**（push/chat/external 型に無し）・**in_app_passive は pull surface であって通知でない**・**silent/observe/internal_prepare/blocked を通知しない**（沈黙中心）・**in_app_passive_eligible は全条件 AND**・**active_prompt 非配信（INV-11）**・unknown ceiling conservative・**opt-in 必須（userInAppSurfaceOptIn・notification opt-in と分離）**・**fatigue cap**・**forbidden identifier 衛生**（配信系語を実装本体に残さない・allowlist key walker）・no external communication・no automatic send・**CEO + production 三重 gate** |
| traceRefs | deliveryDecisionId / interventionDecisionId / snapshotId + suppressedReasons |

### RJ2e Department Responsibility Matrix（CopySurface・user-facing 文面 + exact catalog）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Communication**（文面技術安全）+ **Growth**（voice/tone・CEO 承認済 exact catalog） |
| consultedDepartments | Product（体験文脈）・Permission（露出可否は RJ2d で確定済） |
| blockingDepartments | **CEO**（文面/トーン/世界観の二重 gate・承認済）+ Permission |
| outputs | `RenderedCopyV0`（exact 文面・claim/question copy + generic choice label）・`copyViolations`。**proposal/departure/notification 文面なし** |
| safetyGate | **三層防御**（入力 view precheck throw / 出力 exact catalog 固定 lookup / copyViolations walker）・**dynamic interpolation なし**・**LLM 自由生成なし**・exact template + choice label whitelist・forbidden lexicon scan（verdict/delay-departure/sensitive-work-reservation/probability/action/raw id）・serialization backstop・resolve_overlap は衝突/重複断定なし（RJ1b）・notification/contact/dispatch 型に無し・**CEO 文面承認 gate 通過済** |
| traceRefs | consumer view kind のみ（id/trace なし） |

### RJ2d Department Responsibility Matrix（SurfaceProjection・consumer 境界 + allowlist 構築）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Communication**（consumer projection 境界を所有・copy/配信は HOLD） |
| consultedDepartments | Permission（exposure/redaction）・Risk（claim/question 素材）・Plan/Mobility/Context（subject ref・opaque 化） |
| blockingDepartments | **Permission**（露出可否）+ **CEO**（RJ2e/f 承認必須） |
| outputs | `SurfaceProjectionConsumerViewV0`（consumer 唯一の出口）・`SurfaceProjectionInternalBundleV0`（internal）・2-walker。**user-facing copy/proposal content/departure content/notification なし** |
| safetyGate | **allowlist 構築**（内部 object を spread しない・leak-by-omission 不可）・**ConsumerView/InternalBundle 型分離**・**consumer-safe kind**（4 gate→needs_verification・gate category 非露出）・**id/trace/decision metadata 全除去**・opaque projection-local ref（非相関）・**boundary boolean only**（reason/slice 名/version 非露出）・**serialization backstop**（禁止トークン 22 種）・**5 walker validated binding failure-loud**（BoundSurfaceV0 偽造対策で全層再検査）・consumer view が唯一の consumer 読取り対象（direct read 解禁点）・notification/contact/dispatch 型に無し |
| traceRefs | projectionId / surfacePlanId（**InternalBundle のみ**）。consumer view は opaque ref のみ |

### RJ2c Department Responsibility Matrix（ClarificationQuestion・確認 slot + identity/redaction）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Communication**（question slot を所有・文面/配信は HOLD） |
| consultedDepartments | Permission（gate reasons = question 源・plan exposure）・Risk（time collision/unresolved）・Plan/Mobility/Context（subject node ref・id のみ） |
| blockingDepartments | **Permission**（ask 可否の拒否権）+ **CEO**（RJ2e/d/f 承認必須） |
| outputs | `ClarificationQuestionSetV0`（確認 slot・kind のみ）・2-walker（set/binding）。**user-facing question 文面/proposal/departure/notification なし** |
| safetyGate | **hard gate**(ask_eligible ∧ clarificationOnly ∧ ask_clarification の 3 揃い)・questionTextDraft null（文面 RJ2e HOLD）・**answerShape internal structure only**（label/choices なし）・**leaveBy/departure 逆算質問禁止**（構造遮断）・**unresolved allowlist**（movement/eta/leaveBy/place/source-pending/duplicate を question 化しない）・**exact_time_collision_ambiguous を duplicate 断定しない**（assertsDuplicate=false・relationRef 必須）・**per-event/per-relation identity**（dedupe で source trace 保持）・confirmation_needed claim と分離（relatedClaimRefs は trace hint・mutate しない）・sensitive genericize（category 非露出）・evidence internal_trace_only・questionSet は consumer payload でない・direct read 禁止・notification/contact/dispatch 型に無し |
| traceRefs | questionId / surfacePlanId / interventionDecisionId / snapshotId + relationRef + gateReasonCode + relatedClaimRefs(hint) + evidenceRefs(field-level・internal) + redactionReason |

### RJ2b Department Responsibility Matrix（SurfaceClaim・主張してよい envelope + evidence/redaction）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Communication**（claim envelope を所有・copy/配信は HOLD） |
| consultedDepartments | Risk（fj/crp バケット = claim 素材）・Permission（eligibility redaction/gate・plan exposure）・Plan/Mobility/Context（subject node ref・id のみ） |
| blockingDepartments | **Permission**（redaction/gate 拒否権・露出可否）+ **CEO**（RJ2e/c/d/f 承認必須） |
| outputs | `SurfaceClaimSetV0`（claim envelope）・`BoundSurfaceV0`（validated binding）・3-walker（plan 不接触流用 / claimSet / binding）。**user-facing copy/question/proposal/departure/notification なし** |
| safetyGate | **feasibility_state assert 禁止**（claimType に verdict 無し）・claimTextDraft null（copy RJ2e HOLD）・actionAffordance none（notActionable passive reference）・evidence internal_trace_only（consumer payload に出さない）・exposure none/internal_only で claims []・claim exposureBinding ≤ plan.exposureLevel（userFacingExposureRank）・internal_only は claim 経路に乗せない・sensitive genericize（subject id のみ・category 非露出）・**claimId uniqueness**・**SurfaceClaimSet/BoundSurface は consumer payload でない・direct read 禁止・validated binding 必須**・**bindClaimsToPlan failure-loud**・confirmation_needed ≠ question（ask_eligible のみ・文面なし）・notification/contact/dispatch 型に無し |
| traceRefs | claimId / surfacePlanId / interventionDecisionId / snapshotId + evidenceRefs(field-level・internal_trace_only) + redactionReason |

### RJ2a Department Responsibility Matrix（JudgmentSurfacePlan・surface 露出境界の包絡）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Communication**（surface plan 編成を所有・copy/配信は HOLD） |
| consultedDepartments | Permission（decisionKind/eligibility/actionBoundary 由来）・Risk（fj/crp/prop guard + suppression reason）・Plan/Mobility/Context（node refs） |
| blockingDepartments | **Permission**（露出可否の拒否権）+ **CEO**（RJ2e/f 承認必須） |
| outputs | `JudgmentSurfacePlanV0`（exposure 包絡 + suppression honesty + carry）・`surfacePlanViolations`（walker・CEO 防御ガード込み）。**user-facing copy/proposal/question/departure/notification なし** |
| safetyGate | core 直結禁止（plan 経由・INV-0）・exposure ≤ decisionKind ≤ actionBoundary（INV-4）・internal_only と passive_only 非混（RJ2a-0A）・default-deny（v0 全 ref []）・notActionable で kill しない（INV-9）・clarificationOnly は decisionKind===ask_clarification のみ・departureLineRefs/proposalCandidateRefs v0 []・active_prompt 非配信/dispatch でない（INV-11）・surface object≠display（INV-10）・graphViewerKey を authority/payload に出さない・**actionBoundary cap 防御検証（CEO 追加）**・copy(RJ2e)/notification(RJ2f) HOLD |
| traceRefs | surfacePlanId / interventionDecisionId / eligibilityId / 全 chain id + evidenceRefs(field-level) + missingInputRefs carry |

### RC2c-2 Department Responsibility Matrix（InterventionDecision / ContactPolicy・抑制の層）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Permission**（介入判断の内部状態を所有。文面/提案/通知は出さない＝RJ2+/Communication） |
| consultedDepartments | Risk（Feasibility/Collapse/Propagation）・Plan・Mobility・Context・Communication（将来の接触上限） |
| blockingDepartments | **Permission** |
| outputs | InterventionDecisionV0（decisionKind + contactPolicy + deliveryModeCeiling + actionBoundary[carry] + whyNow/whyNotFactors + nextEvaluationAt/reevaluationTrigger/stopCondition[observe] + InterventionDecisionTrace） |
| missingInputs | permission origin / source revision / redaction visibility / confirmation target / duplicate identity を whyNotFactors + missingInputRefs carry で保持 |
| safetyGate | **no action/notification/proposal/copy**・**decisionKind ≤ actionBoundary ceiling**・**silent≠observe**（observe のみ再評価条件）・unknown は ask に寄せない・gate 下で internal_prepare-to-action しない・ambiguity は move/skip にしない・high collapse/infeasible は通知/write_anchor にしない・contactPolicy/deliveryModeCeiling は配信命令でない（v0 配信なし）・display redaction gate 継承・LLM 不使用・trace 必須 |
| traceRefs | InterventionDecisionTrace.usedInputRefs + eligibilityRefs（eligibilityId/level/boundary）+ evidenceRefs + missingInputRefs + 全 chain id |
| 実装した責務 | evaluateInterventionDecision（warranted rank を boundary cap で制限・silent/observe 分離・gate 下 prepare 抑止・observe 再評価条件・integrity guard 7 段[snapshotId×4 + chain×3]） |
| 実装しなかった backlog | **RJ2+ 判断文/提案/出発線/intervention ladder（別 GO）**・実 nextEvaluationAt timestamp（pure core は clock なし・subjective minute のみ）・contact delivery（Communication 部署・通知/push/external は永久 gate）・reevaluation scheduler（B2/R6） |

### RC2c-1 Department Responsibility Matrix（InterventionEligibility / ActionBoundary・許可の層）

| 項目 | 内容 |
|---|---|
| owningDepartment | **Permission**（介入可否=action boundary を所有。提案文/実行は出さない＝RJ2+/Communication） |
| consultedDepartments | Risk（Feasibility/Collapse/Propagation）・Plan・Mobility・Context（commitment gate）・Communication（external draft 境界） |
| blockingDepartments | **Permission**（拒否権の正本） |
| outputs | InterventionEligibilityV0（eligibilityLevel + canSuggest{7} + requiresConfirmation/ExternalCommunication + actionBoundary + blockedReasons/confirmationReasons + displayRedactionRequired + confidence + EligibilityTrace） |
| missingInputs | permission origin / otherPeople / reservation / payment / work / external communication / redaction visibility / sourceRevisionPending を blockedReasons/confirmationReasons + missingInputRefs carry で保持 |
| safetyGate | **現実 read は行動許可でない**・**default-deny（unknown→allowed にしない）**・otherPeople/reservation/payment/work/sensitive 強 gate→requires_confirmation 以上・exact_time_collision_ambiguous は自動 move/skip にしない・high risk/infeasible は実行許可でない・**v0 で write_anchor/send_message/book_pay/external_communication を天井にしない**・sourceRevisionPending は permission を緩めない・display も redaction gate・memory/correction で confirmation を消さない・no action/proposal・LLM 不使用 |
| traceRefs | EligibilityTrace.usedInputRefs（field-level）+ evidenceRefs + missingInputRefs + feasibilityJudgmentId/collapseRiskProfileId/collapsePropagationId |
| 実装した責務 | evaluateInterventionEligibility（gate 集約[最 restrictive]・default-deny precedence・canSuggest 面・actionBoundary 天井制御・integrity guard 6 段[snapshotId×3 + chain×2 + scope]）。ern に sensitive:boolean 露出 |
| 実装しなかった backlog | **RJ2+ 判断文/提案/3案/出発線/intervention ladder（別 GO）**・permission origin の実供給（v0 は ern.permissionLevel）・external communication draft（Communication 部署・draft_only 永続禁止）・sensitiveCategory 別 gate（v0 は boolean）・memory/correction gate（B1 配線後） |

### RC2b-2 Department Responsibility Matrix（CollapsePropagation impact surface・波及の層）

| 項目 | 内容 |
|---|---|
| owningDepartment | Risk（波及範囲=impact surface の整理を所有。判断文/提案/出発線は出さない＝RJ2+） |
| consultedDepartments | Plan・Mobility・Context（event order/relation/movement）。Permission は action boundary（propagation source にしない） |
| blockingDepartments | Permission |
| outputs | CollapsePropagationMapV0（propagationLevel + propagationEdges[directional・from/to/resolved] + affectedNodeRefs + downstreamImpactCandidates + carryoverCandidates[v0 []] + unresolvedPropagationInputs + trace） |
| missingInputs | route/ETA/leaveBy/place/source revision/exact-time identity/carryover model を unresolvedPropagationInputs + missingInputRefs carry で保持 |
| safetyGate | **CollapseRisk と別軸**・確率/%なし・**no causality 断定**（ambiguous は edge なし）・movement unresolved は delay 確定にしない（resolved:false）・**known surface は conflict 由来のみ**（commitment/permission/missing で surface 化しない）・**directional edge は sorted id 不使用**（from/to 保持・backward propagation なし）・no proposal/action・trace 必須・LLM 不使用 |
| traceRefs | CollapsePropagationTrace.usedInputRefs（field-level）+ relationRefs + failureModeRefs + affectedNodeRefs + feasibilityJudgmentId + collapseRiskProfileId |
| 実装した責務 | evaluateCollapsePropagation（conflict→known surface[earlier→later directional + adjacent downstream]・movement→候補 edge[resolved:false]・ambiguous→edge なし unknown・directional edgeId・integrity guard 3 段[snapshotId×2 + fjId]） |
| 実装しなかった backlog | **RJ2+ 判断文/提案/出発線（別 GO）**・same_day_carryover_candidate_edge（cross-day model 待ち）・decision_dependency_edge（dependency model 待ち）・directional relation の一般化（movement/causality/delay/responsibility） |

### RC2b-1 Department Responsibility Matrix（CollapseRisk failure mode map・崩れ方の層）

| 項目 | 内容 |
|---|---|
| owningDepartment | Risk（崩れ方=failure mode の整理を所有。判断文/提案/出発線は出さない＝RJ2+ の責務） |
| consultedDepartments | Plan・Mobility・Context（feasibility 経由の reasons/relations）。Permission は action boundary context（collapse source にしない） |
| blockingDepartments | Permission |
| outputs | CollapseRiskProfileV0（riskLevel[factor aggregation・%なし] + failureModes[category/riskContribution/evidenceRefs/missingInputRefs/relationRefs/sourceRefs] + riskFactors/unresolvedCriticalInputs carry + pairwiseRelationRefs + confidence + CollapseRiskTrace） |
| missingInputs | route/ETA/leaveBy/place/source revision/duplicate(exact-time) identity 等を failure mode の missingInputRefs + carry した snapshot missingInputRefs で保持 |
| safetyGate | **Feasibility と別軸（feasibilityStatus 非参照/非コピー）**・確率/%なし・proposal/出発線/intervention/通知/action/permission 緩和なし・**missing/commitment/permission/decisionDebt だけで high にしない**・exact_time_collision_ambiguous=duplicate でない・fake ETA/leaveBy/prep なし・knownComponentSummary 非参照・LLM 不使用・trace 必須 |
| traceRefs | CollapseRiskTrace.usedInputRefs（field-level）+ relationRefs（pairwise）+ feasibilityJudgmentId + missingInputRefs（source trace） |
| 実装した責務 | evaluateCollapseRisk（feasibility reasons/relations → failure mode 再投影・category 分離・contribution max で riskLevel・integrity guard[snapshotId 一致]） |
| 実装しなかった backlog | **RJ2+ 判断文/提案/出発線/intervention ladder（別 GO）**・collapse propagation（遅延波及）・duplicate model 本格化・per-viewer・directional relation（movement/causality 等は sorted-id 流用不可・directional id 要） |

### RJ1b Department Responsibility Matrix（day-scope 集約 + duplicate safeguard）

| 項目 | 内容 |
|---|---|
| owningDepartment | Risk（day-scope の成立性集約を所有。判断文/提案/出発線は出さない） |
| consultedDepartments | Plan・Mobility・Context（event-level 材料）。Permission は action gate context（feasibility を緩めない） |
| blockingDepartments | Permission |
| outputs | Day-level FeasibilityJudgmentV0（merged buckets 集約）+ RealityJudgmentTrace（**perEventContributions** で day→event）+ duplicate_identity_unresolved（safeguard） |
| missingInputs | duplicate/equivalent 未確定（duplicate_identity_unresolved）+ sourceRecordRevisionPending/sourcesRevisionPending + place/route/ETA/leaveBy + event-level unresolvedCriticalInputs |
| safetyGate | **confirmed 1 つでも→day infeasible / unresolved→day unknown**・unknown 非断定・**duplicate/equivalent 未確定で confirmedBlocking にしない**・no probability/% / no proposal/action・trace 必須・LLM で同一判断しない・permission は feasibility 不可にしない |
| traceRefs | perEventContributions（day→event status/risk）→ reason.targetNodeId（event）→ evidenceRefs（field-level node#field） |
| 実装した責務 | per-event 評価→merged 集約・isPossibleDuplicate（同一 timeWindow safeguard）・perEventContributions trace・source revision pending 維持 |
| 実装しなかった backlog | **RJ2+ 判断文/提案/出発線（別 GO）**・duplicate model 本格化（externalUid/title hash/location hash/source dedup・ern 露出後）・day-scope の時間軸精密 filter・CollapseRisk（RC2b） |

### RJ1a Department Responsibility Matrix（Feasibility 純粋判定器・判断の層）

| 項目 | 内容 |
|---|---|
| owningDepartment | Risk（成立性=破綻可能性の判断を所有。判断文/提案/出発線は出さない＝RJ2+/Communication の責務） |
| consultedDepartments | Plan・Mobility・Context（ern/cs/mv を読む）。**Energy・Memory は capability pending**（snapshot 経由）。Permission は action gate context として参照（feasibility を緩めない） |
| blockingDepartments | Permission（判断は出すが、action の可否は Permission が拒否権。feasibility ≠ action 許可） |
| outputs | FeasibilityJudgmentV0（feasibilityStatus + riskLevel + 4 バケット[confirmed/inferred/unresolvedCritical/riskFactors] + judgmentConfidence[質的・%なし] + displayPolicy + missingInputs/Refs carry + RealityJudgmentTrace） |
| missingInputs | unresolvedCriticalInputs（place_resolution_pending / movement_requirement_unknown / eta_source_missing / route_unresolved / leave_by_unresolved）+ snapshot missingInputRefs carry + sourcesRevisionPending / sourceRecordRevisionPending |
| safetyGate | **confirmed のみ infeasible**・unknown 非断定（feasible/infeasible どちらにも倒さない）・**確率/%なし**・proposal/出発線/intervention/permission 緩和を出さない・knownComponentSummary 非参照・LLM 不使用・commitment/permission/decisionDebt high を infeasible/late 確定にしない・trace 必須・id≠内容証明 |
| traceRefs | RealityJudgmentTrace.usedInputRefs（ern/cs/mv id）+ missingInputRefs（source trace 不失）+ graphBaseId/snapshotId/inputRevisionSet/derivationVersionSet |
| 実装した責務 | evaluateFeasibility（4 バケット分離・event/day scope・hard overlap = confirmed 構造衝突・soft overlap = inferred・movement/place/departure を unresolved vs inferred に分類・factor 集約 riskLevel・RealityJudgmentInput 単一正本） |
| 実装しなかった backlog | **RJ2+ 判断文/提案/3案/出発線/intervention ladder（別 GO）**・sources-map revision 配線（sourceType 実質不変で当面 pending）・moment_forward scope の精密 time フィルタ（v0 は構造 overlap）・CollapseRisk（RC2b）・per-viewer projection |

### RC2a-6 Department Responsibility Matrix（Graph root assembler・identity と編成の層）

| 項目 | 内容 |
|---|---|
| owningDepartment | Plan/Risk joint（compile 済み材料を graph root に編成。判断は出さない＝RJ1 の責務） |
| consultedDepartments | Mobility・Context（ern/mv/cs を full object で束ね）。**Energy・Memory は capability pending として missingInputRefs に表現**（pipeline_capability・runtime 部署名なし）。Permission は consumer 側 |
| blockingDepartments | Permission（graph は判断しないが、消費する RJ で permission が拒否権） |
| outputs | RealityGraphSnapshotV0（2 層 identity[graphBaseId day-level / snapshotId minute-level] + viewerScope[擬名化・raw viewerId なし] + builtAt[momentSnapshot.instant carry] + ern/mv/cs full[id canonical 整列] + decisionDebt[単一正本=momentSnapshot.decisionDebt] + momentSnapshot + inputRevisionSet + derivationVersionSet + pendingInputs + missingInputs/missingInputRefs[root まで失わず carry + graph-level pending refs] + safetyFlags + integrityViolations + sourceRefs[dayGraphSnapshotId + momentSnapshotCacheKey trace]） |
| missingInputs | momentSnapshot から carry（place/eta/origin/change 等）+ graph-level pending revisions（day_state_record/environment/hints/shift *_pending を pipeline_capability で） |
| safetyGate | **identity 規律**（2 層・momentSnapshotCacheKey を base に入れない[分依存→base が毎分変わる矛盾を回避]・id≠内容証明・raw viewerId 不混入・builtAt 秒/ms 非 identity・array index 非依存・duplicate id fail・材料整合性 guard）/ **判断結果を出さない**（Feasibility/Proposal/DepartureLines/Permission action 不在）/ unknown 非 0 / missingInput trace 保持 / runtime Department object なし / LLM・UI・IO 不接触 |
| traceRefs | RJ1 が RealityJudgmentInput として消費（JudgmentTrace.usedInputs に snapshotId + full inputRevisionSet + missingInputRefs。cacheKey 短縮 hash 単独で同一性を決めない） |
| 実装した責務 | assembleRealityGraph（2 層 identity 完成・既存 buildGraphBaseId/buildSnapshotId helper 再利用・InputRevisionSet 集約[real where available / 未供給は pending sentinel]・derivationVersionSet 可視化・missingInputRefs root carry + pending refs・safetyFlags[実データ検証 + 構造 tripwire]・canonical node 整列・全 node duplicate guard + 材料整合性 guard） |
| 実装しなかった backlog | **RJ1 Feasibility（次 GO）**・RealityJudgmentInput 正式型（RG0.6 backlog #1）・DayStateRecord/weather/hints/shift の実供給（pending sentinel）・per-viewer projection（RG0.6 §11）・~~dayGraphSnapshotId 内容完全化~~（**RC2a-6A で解消済**） |

### RC2a-5 Department Responsibility Matrix（input-bundle snapshot・判断入力の地面）

| 項目 | 内容 |
|---|---|
| owningDepartment | Plan/Risk joint（瞬間の判断入力を束ねる。Feasibility は出さない＝RJ1 の責務） |
| consultedDepartments | **Plan・Mobility・Context・Risk**（compile 済み材料 = ern/mv/cs/decisionDebt を参照束ね）。**Energy・Memory は未接続**だが **runtime に部署 enum を載せない**（RC2a-5B §1）— 上流 compile 材料の未供給を `missingInputRefs` の `pipeline_capability`（energy_projection_pending / fatigue_projection_pending / memory_profile_pending / correction_profile_pending）として表現。Permission は consumer 側 |
| blockingDepartments | Permission（snapshot は判断しないが、消費する RJ で permission が拒否権） |
| outputs | MomentStateSnapshotV0（schemaVersion + deriveMomentSnapshotVersion + **momentSnapshotCacheKey**[cache key・内容証明でない] + inputRevisionSetPending:true + RealityInstant carry + relevantNodes[active/next/past/upcoming/unresolvedMovement/boundarySpanning] + nodeRefs[ern/mv/cs **id join**] + decisionDebt + missingInputs codes + **missingInputRefs（source trace・部署名でなく node種別/pipeline_capability）** + derivationVersions）。**unconnectedDepartments runtime field は廃止**（RC2a-5B §1） |
| missingInputs | 各ノードのものを失わず集約（place_resolution_pending / eta_source_missing / origin_inference_pending / change_candidate_pending 等）＋上流未供給（energy/fatigue/memory/correction *_pending）。**missingInputRefs で source node/field + dedupeKey を保持**（dedup で trace を失わない・criticality は v0 unknown） |
| safetyGate | **判断結果を出さない（Feasibility/CollapseRisk/3案/出発線なし）**・unknown 非 0・single score 化しない・permission 非緩和・knownComponentSummary は RJ 正本入力にしない・mv absence≠移動不要・LLM 不使用・browser TZ 非依存（JST 強制）・id≠内容証明・**部署 enum を runtime 正本にしない**・全 node 種別 duplicate id guard |
| traceRefs | RJ1 が RealityJudgmentInput としてこの snapshot を消費（JudgmentTrace.usedInputs に momentSnapshotCacheKey + missingInputRefs） |
| 実装した責務 | deriveMomentSnapshot（RealityInstant carry・relevant nodes 整理[主観分 05:00 境界]・id join 束ね[ern/mv/cs 全 duplicate guard]・missingInputs source trace 保持[dedupeKey/criticality]・境界跨ぎ event 分離 + unsupported trace・上流未供給を pipeline_capability で表現） |
| 実装しなかった backlog | **RJ1 Feasibility（次 GO）**・RealityJudgmentInput の正式型（RG0.6 backlog #1）・**Energy/Memory compile 材料**・cross-day/carryover/fatigue projection（B1/将来）・日跨ぎ単一 event（DayGraph end_before_start で表現不可＝既知制約） |

### RC2a-4 Department Responsibility Matrix（Risk / Plan joint）

| 項目 | 内容 |
|---|---|
| owningDepartment | Risk / Plan joint（未決の集約を Risk が統合・Plan が予定材料を提供） |
| consultedDepartments | Plan・Mobility・Context・Memory・Permission |
| blockingDepartments | Permission（debt 高でも自動介入は permission が拒否） |
| outputs | decisionDebt 8 components（placeDebt/timeDebt/mobilityDebt/confirmationDebt/candidateDebt/followupDebt/changeDebt/snoozeDebt）+ knownComponentSummary（debugOnly）+ MomentDecisionContext（RealityInstant carry + activeWindow + nextRelevantNodeIds + missingInputs） |
| missingInputs | origin_inference_pending・request_frame_pending・place_candidate_pending・communication_followup_pending・intervention_reaction_pending・eta_source_missing・drift_tracking_pending |
| safetyGate | **unknown を 0 にしない**・**single score に潰さない**・**permission 非緩和（permission field 無し）**・**mv absence を mobilityDebt 0 と読まない**・LLM 不使用 |
| traceRefs | （RJ 接続時）JudgmentTrace.usedInputs に decisionDebt components・MomentDecisionContext |
| 実装した責務 | decisionDebt の成分分解（**3 成分 derivable**: place/time/mobility / **5 成分 unknown**: confirmation/candidate/followup/**change**/snooze）+ RealityInstant を carry する Moment 統合。RC2a-4A で changeDebt を unknown に修正（commitment は debt source でなく severity modifier） |
| 実装しなかった backlog | RJ1 Feasibility（別 GO）/ confirmationDebt 実値（OriginInference 後）/ candidate/followup/snooze 実値（Request/Communication/Reaction source 後）/ changeDebt 実値（変更候補/drift tracking = Stage 2 後）/ placeResolutionDebt（RC4 場所解決後） |

### RC2a-3 Department Responsibility Matrix（Context）

| 項目 | 内容 |
|---|---|
| owningDepartment | Context |
| consultedDepartments | Plan（予定列）・Memory（補正履歴・将来 B1） |
| blockingDepartments | Permission（commitment 高でも自動変更は permission が拒否） |
| outputs | rigidity（RealityAttribute・動かしにくさ判断・RC2a-3A で裸値→RealityAttribute 化）/ protectionReasons / otherPeoplePossible / workOrShiftPossible / reservationOrPaymentPossible / fixedStart / deadlineOrCarryoverImpact / socialWeight / changeCost（全て RealityAttribute・fixedStart/rigidity/socialWeight/changeCost/permission は各々分離） |
| missingInputs | other_people_unknown / reservation_payment_unknown / deadline_model_pending（task/Deadline 未実装）/ commitment_signal_weak |
| safetyGate | unknown を low 扱いしない・title だけで断定しない・**commitment ≠ permission（permission field を持たない）** |
| traceRefs | （RJ 接続時）JudgmentTrace.usedInputs に cs node refs（targetNodeId=ern で join） |
| 実装した責務 | commitmentSignal compile（守る理由・他人/予約/勤務の可能性・固定度・社会的重み）。Context 部署の最初の実体化 |
| 実装しなかった backlog | 予定の「意味/目的」独立判断（同じ 19:00 渋谷でも飲み/商談/病院で変わる）→ 将来 Context 拡張 / deadlineOrCarryoverImpact 実値（TaskRealityNode・Deadline Model 後） |
