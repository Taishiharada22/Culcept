# 候補レンズ UI — 参照画像 redline 仕様（実装前・設計提出 / GO 待ち）

> 2026-06-15 / Build Unit / CEO「理想画像を“設計図”として固定し、そこに準拠して再構築。実装修正でなく、参照画像準拠の再設計→実装に切替」
> ★本書は **add-search1/2/3.png を唯一の正（reference of truth）** として、①②③を「構造・主役・情報階層・寸法・ボタン・余白・遷移・現実装との差分」に分解した **UI 仕様書**。非トレース・画像切貼り・写真添付は禁止、コンポーネントとして再構成する。
> ★寸法は **候補探索領域の内容幅 W = 340px**（予定追加シートの内容幅・full-width を占有）を基準に px 表記。

---

## 0. 全体方針（GPT 指摘の反映）
- **主役の転換**: 「予定追加UI＋脇に小さい候補カード」→ **「候補探索付き予定追加UI」**。flag ON のとき、予定追加UIの候補領域を **reference 準拠 layout に丸ごと置換**する（既存フォームの脇に差し込まない）。
- **①②③は枠の意味が違う**: ①=選び始める / ②=詳細理解 / ③=比較。状態遷移を強くする。
- **②はボトムシートではない**: ①と同じ領域の中で候補カードが**その場で詳細化**する（前回 REDO-4 の「下から別 UI」は誤り→撤回）。
- **③は全画面ページでもない**: **比較専用の大きな状態**（比較モーダル／大きめサーフェス）。主役は**比較表**。前回 REDO-4 の「アプリ全画面ページ」は誤り→撤回。
- **honesty（不変）**: 写真・地図・Wi-Fi・電源・静か・雰囲気・営業時間は**未確認＝捏造しない**。reference がそれらを「確定」風に出している箇所は、**abstract タイル**／**未確認注記**に置換（§4 に正直な差分を明示）。

---

## 1. レイアウトシステム（数値化）
| トークン | 値 | 備考 |
|---|---|---|
| 内容幅 W | 340px | 候補領域は full-width 占有 |
| カード角丸 | 20px (`rounded-[20px]`) | ①②共通 |
| カード内余白 | 16px | |
| ブロック間 余白 | 12px | |
| サムネ（①） | 72×72px 角丸16 | 写真でない abstract タイル |
| メディア（②） | W×150px banner | abstract タイル |
| ボタン高 | 44px | tap target |
| 比較表 行高 | 52px | ③ 主役 |
| フォント階層 | 名前16/理由13/チップ12/住所11.5/補助10 | px |

---

## 2. State A = ① 候補が出る（選び始める）
**画面構造（縦）**
```
おすすめの候補                               ← 見出し 14px bold
┌─────────────────────────── card (W, 高さ ~232px) ──┐
│ [72×72 thumb]  名前 16/2行         [相性高め]badge │  ← 上段: サムネ左 + 名前右 + 相性右上
│                理由文 13/2行                        │
│ 🚶約5分(目安)  🏷️カフェ  💜相性                      │  ← 主要属性チップ（honest のみ）
│ 📍 東京都江東区平野1-4-8 …                           │  ← 住所 11.5
│ [ 詳細を見る  outline 50% ][ 比較する ⇧ filled 50% ]│  ← 比較導線
└────────────────────────────────────────────────┘
   └ 次候補カードの上端が ~40px のぞく（1主役 + 次少し）
```
- **主役要素**: いま見ている **1 候補カード**。
- **情報階層**: 名前 > 相性 > 理由 > 主要属性 > 住所 > アクション。
- **カードサイズ**: W×~232px。サムネ 72px。**小さすぎ禁止**＝文字を詰めない・行間 1.5。
- **ボタン配置**: カード下端に 2 ボタン横並び（各 ~50%、44px）。
- **余白**: カード間 12px、内側 16px。
- **状態遷移**: 縦 snap（`snap-mandatory`）で 1 候補ずつ送る。下に次候補が少し見える高さ（container ≈ card+40px peek）。
- **現実装との差分**: 現状はカードが小さく文字が詰まり、情報階層が崩れている。→ サムネ拡大・理由を読めるサイズ・チップを整理・1主役+のぞきを明確化。

---

## 3. State B = ② タップで詳細がひらく（詳細理解 / ★その場展開・ボトムシートにしない）
**画面構造**: ①の **同じカードが同じ位置で縦に拡張**（accordion）。周囲（フォーム・他候補）はそのまま、当該カードだけ大きくなる。
```
┌─────────── card 展開 (W, 高さ ~452px) ───────────┐
│ 名前 17                                   [∧ 畳む]│
│ カフェ ·  [相性高め]                                │
│ ┌──── abstract media banner  W×150 ────┐          │  ← 地図/写真の位置（honest: abstract）
│ └──────────────────────────────────────┘          │
│ 📍 東京都江東区平野1-4-8 清澄白河フラッグA棟         │
│ 🚶約5分(目安)[計算]  🏷️カフェ[事実]  💜相性          │  ← 属性 + evidence
│ ┌ なぜここを選ぶ？ ──────────────────────┐         │
│ │ 徒歩約5分(目安)で、会議前に余白を…(よく行く)│         │
│ └────────────────────────────────────────┘         │
│ [ 閉じる ‹ 50% ][ 比較する ⇧ 50% ]                  │
└────────────────────────────────────────────────┘
```
- **主役要素**: 展開した候補の **詳細（メディア＋なぜ＋属性）**。
- **情報階層**: 名前 > メディア > 住所 > 属性(evidence) > なぜここを選ぶ？ > アクション。
- **カードサイズ**: W×~452px（①の ~232 から **その場で拡張**）。メディア W×150。
- **ボタン配置**: 下端に 閉じる / 比較する。
- **余白**: なぜブロック内 16px、メディア上下 12px。
- **状態遷移**: ①の「詳細を見る」or カードタップ → 当該カードが拡張（高さアニメ）。∧/閉じるで①へ収縮。**別サーフェスは出さない**。
- **現実装との差分**: REDO-4 は下から出る別ボトムシート＝**誤り**。→ **同領域内のインライン詳細展開**に戻す（REDO-3 の方向が正しかった）。

---

## 4. State C = ③ 候補を比較する（比較専用の大きな状態 / ★全画面ページにしない）
**画面構造**: 比較**専用サーフェス**（大きめの比較モーダル／sheet・角丸・周囲を暗転）。アプリ全画面ページ化はしない。主役は**比較表**。
```
┌──────── 比較サーフェス (大きめモーダル) ────────┐
│ ‹ 戻る      候補を比較          会議前 / 2件     │  ← ヘッダー
│ ┌ 候補A ────────┐ ┌ 候補B ────────┐ ▲▼        │  ← 2候補ヘッダー(サムネ+名前) + 入替
│ │[mini] 名前      │ │[mini] 名前     │           │
│ └────────────────┘ └────────────────┘           │
│ ── 候補を比較 ────────────────────────────────  │
│ 🚶 徒歩    │ ✓約5分(目安)紫 │ 約15分(目安)        │  ← 比較表(主役)・優位セル紫+✓
│ 💜 相性    │ よく行く…      │ —                  │
│ 🏷️ 種別    │ カフェ         │ カフェ              │
│ 📍 住所    │ 江東区…        │ 渋谷区…             │
│ ── ✨ おすすめ ───────────────────────────────  │  ← おすすめサマリー
│ ブルーボトル が、徒歩の点で合いそうです            │
│ [   この場所にする（選択時のみ・下部固定）   ]    │  ← CTA
└────────────────────────────────────────────────┘
```
- **主役要素**: **比較表**（2 候補 × 目的レンズの軸）。
- **情報階層**: 2候補ヘッダー > 比較表 > おすすめサマリー > CTA。
- **寸法**: ヘッダー 56px、候補ヘッダー 88px、表 行高 52px、おすすめ 72px、CTA 56px。サーフェスは内容に応じ ~70–85vh の比較モーダル（full-bleed のアプリページにしない）。
- **ボタン配置**: 上＝戻る、右＝目的/件数、下＝確定 CTA（候補タップで選択→確定）。
- **余白**: 行 padding 縦12/横14、ブロック間 16。
- **状態遷移**: ①/②の「比較する」→ 比較サーフェスが前面に。戻るで①へ。右候補は ▲▼/スワイプで入替。
- **現実装との差分**: REDO-4 は `fixed inset-0` の**アプリ全画面ページ**＝CEO 意図とズレ。→ **比較専用モーダル（大きめ・角丸・暗転）**に変更。主役は比較表。

---

## 5. ★honest deviation（reference と意図的に変える点・捏造回避）
| reference の要素 | 本実装（honest） | 理由 |
|---|---|---|
| 写真サムネ | **abstract タイル**（category 色＋📍） | 外部 API なし・写真捏造禁止 |
| map preview | **abstract タイル**（地図画像は出さない） | 外部 API なし・地図捏造禁止 |
| Wi-Fi/電源/静か/雰囲気/営業時間の「あり/値」 | **主表に出さず**、目的に関わる物だけ「未確認」と小さく注記 | データ不在＝捏造しない（CEO「正直なデータのみ」） |
| 優位ハイライト | **表示値に差がある行のみ** | 見えない score 微差で主張しない（既存 honesty） |
| 徒歩などの計算値 | 「約/目安」表記＋evidence ラベル | haversine 直線ゆえ |

→ この 5 点は reference と見た目が変わるが、**CEO の honesty 原則に従う必須の差分**。GO 時に「ここは画像と変えてよいか」を確認したい。

---

## 6. reference 対応表（実装後に実スクショで提示する観点）
| 状態 | reference | 再現する要素 | 既知のズレ（honest deviation） |
|---|---|---|---|
| ① | add-search1 | 見出し/サムネ/名前/相性/理由/主要属性/住所/2ボタン/1主役+のぞき | 写真→タイル・map→タイル・Wi-Fi等は出さない |
| ② | add-search2 | その場展開/大メディア/住所/属性/なぜ/比較導線 | 写真・地図→タイル・未確認属性は出さない |
| ③ | add-search3 | 2候補ヘッダー/比較表(主役)/優位ハイライト/おすすめ/CTA | 雰囲気/Wi-Fi/電源/静か/営業時間の行は honest 版（未確認は注記） |

---

## 7. 実装範囲（GO 後）
- flag ON 時のみ、予定追加UIの候補領域を本仕様の layout に置換（flag OFF/production は既存 `<section>` 不変・hard block）。
- ②=インライン詳細展開（同領域）。③=比較専用モーダル（大きめ・暗転・table 主役）。
- pure resolver / evidence / honesty は不変。DB / migration / 外部 API / preference 保存なし。
- 実装後は **実スクショ + 本対応表** を提示。

## ★stop gate（本書は設計のみ）
本書は設計提出。**実装は CEO の GO 後**。GO 時に §5 の honest deviation の可否も確認する。
