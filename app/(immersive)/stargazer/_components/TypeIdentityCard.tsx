// app/stargazer/_components/TypeIdentityCard.tsx
// 旧12星座タイプ表示コンポーネント — 旧タイプシステム除去済みのためスタブ化
"use client";

interface Props {
  resolvedType: unknown;
  personalityProfile: unknown;
  dimensionDetails: unknown[];
  observationCount: number;
  lightMode?: boolean;
  combinedIdentity?: unknown;
}

export default function TypeIdentityCard(_props: Props) {
  // Old type system has been removed.
  return null;
}
