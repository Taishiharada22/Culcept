import GenomeCardPageClient from "./GenomeCardPageClient";

export const metadata = { title: "Genome Card | Aneurasync" };

export default function GenomeCardPage() {
  return <GenomeCardPageClient />;
}
